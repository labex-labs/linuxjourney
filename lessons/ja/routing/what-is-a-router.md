---
lesson_id: "what-is-a-router"
course_id: "routing"
lang: "ja"
order_index: 1
title: "ルーターとは？"
description: "ルーターが次ホップを選び、ネットワーク間で IP パケットを転送する仕組みを学びます。"
meta_title: "ルーターとは？ - ルーティング"
meta_description: "ネットワークにおけるルーターの役割を理解するための初心者向けガイド。ルーティング、パケットスイッチング、ホップ、ルーターがルーティングテーブルを使用してネットワーク間でデータを転送する方法について学びます。Linux ネットワーキング学習に不可欠なネットワークガイドです。"
meta_keywords: "ルーター, ネットワーキング，ルーティング，ホップ，パケットスイッチング，Linux ネットワーキング，初心者チュートリアル，ネットワークガイド"
---

ルーターはネットワーク層の領域を接続し、その間で IP パケットを転送します。Linux ホストも、転送を有効にし、インターフェース、ルート、近隣探索、フィルタリングポリシーを適切に設定すればルーターとして動作できます。

## ルーティングと転送

ルーティングは、到達可能なプレフィックスに関する情報を構築または選択します。転送はその情報を各パケットへ適用します。宛先を調べ、利用可能なルートと次ホップを選び、ホップ上限を減らし、送信インターフェースから送り出します。

両者は、コントロールプレーンとデータプレーンという別々の関心事です。ルートが存在してもファイアウォールポリシーが転送を遮断することがあり、転送インターフェースが有効でも、有効なルートがない場合があります。

:::single-choice{#router-forwarding-role}
パケット転送は何をしますか？

::option[ルーティング情報を適用し、パケットを次ホップへ向けて送る。]{#router-apply-route .correct explanation="転送は、選択されたルートとポリシーに基づいてパケットごとに行う処理です。"}
::option[すべての宛先に対して永続的なアプリケーションログインを作る。]{#router-create-login explanation="ルーティングはリモートアプリケーションのアカウントを管理しません。"}
::option[ルートがないとき、全インターフェースへパケットをコピーする。]{#router-flood-no-route explanation="通常の IP 転送では、ルーティング不能なパケットを破棄し、代替として Ethernet のようなフラッディングは行いません。"}
:::

## ルーティングテーブルとデフォルト

ルートは、宛先プレフィックスを送信インターフェース、次ホップ、メトリック、送信元の優先設定などの属性と関連付けます。最長プレフィックス一致では、利用可能な中でより具体的なルートが優先されます。IPv4 の `/0` または IPv6 の `::/0` であるデフォルトルートは最も具体性の低い一致で、より具体的なルートが選ばれない場合だけ使われます。

利用可能なルートがなければ、ルーターはパケットを破棄し、ICMP unreachable メッセージを生成する場合があります。デフォルトルートは必須ではなく、公開インターネットを直接指す必要もありません。

:::single-choice{#router-default-route}
デフォルトルートが選択されるのはいつですか？

::option[宛先固有のプレフィックスを確認する前。]{#router-default-first explanation="利用可能なより具体的なプレフィックスが優先されます。"}
::option[パケットが Ethernet ブロードキャストの場合だけ。]{#router-default-broadcast explanation="IP ルートの選択は、ネットワーク層の宛先に基づきます。"}
::option[一致する、より具体的な利用可能ルートがない場合。]{#router-default-fallback .correct explanation="長さ 0 のプレフィックスは、最も具体性の低いルートです。"}
:::

## ローカル通信とルーティングされる通信

同じオンリンクサブネット上の二つのホストは通常、IP パケットをルーターへ送らずにフレームを直接交換します。ルート選択でルーターが次ホップに選ばれる場合や、トポロジーとポリシーによって意図的にルーター経由を強制する場合に、ルーターが関与します。

家庭用「ルーター」は通常、IP ルーター、Ethernet スイッチ、Wi-Fi アクセスポイント、DHCP サービス、NAT、ファイアウォールを組み合わせています。各機能は別々に診断してください。

:::single-choice{#router-same-subnet-path}
同じリンク上の二つのホスト間通信は、必ずデフォルトルーターを通る必要がありますか？

::option[はい。すべてのパケットが WAN ポートへ到達する必要があるからです。]{#router-always-wan explanation="同一リンクへのローカル配送は、リンク上で直接行えます。"}
::option[はい。両方のホストが公開アドレスを持つ場合を除きます。]{#router-public-required explanation="公開・プライベートの区分は、基本的なオンリンク転送を決めません。"}
::option[いいえ。送信側はローカルリンク上の宛先を直接指定できます。]{#router-direct-on-link .correct explanation="ルーティングテーブルが、接続済みプレフィックスをオンリンクとして識別します。"}
:::

## ホップとループ防止

ルーティングされたホップとは、ネットワーク層での一回の転送です。IPv4 の TTL と IPv6 の Hop Limit は各ルーターで減らされ、ループに上限を設けます。ホップ数は距離や品質を完全には表しません。リンクごとに帯域幅、遅延、損失、ポリシー、混雑が異なります。

:::single-choice{#router-hop-count-limit}
ホップ数が少なくても保証できないものはどれですか？

::option[少なくとも一回のルーティングされた転送があること。]{#router-hop-exists explanation="正のホップ数は、ルーターを経由したことを直接示します。"}
::option[アプリケーションにとって高速または良好な経路であること。]{#router-hop-not-quality .correct explanation="ルーターが少なくても、低速、混雑、またはポリシーで制限されたリンクを通る場合があります。"}
::option[ホップ上限フィールドが有限であること。]{#router-hop-limit-finite explanation="これらのフィールドはプロトコル設計上有限です。"}
:::

## まとめ

これで、ルーターによるルート選択と転送処理を区別できます。

1. ルーターを IP ネットワーク間の転送機器として定義する。
2. コントロールプレーンのルーティングとデータプレーンの転送を区別する。
3. デフォルトルートを最も具体性の低いフォールバックとして扱う。
4. ホップ数だけでは経路品質を測れないと理解する。
