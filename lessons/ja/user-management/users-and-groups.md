---
lesson_id: "users-and-groups"
course_id: "user-management"
lang: "ja"
order_index: 1
title: "ユーザーとグループ"
description: "Linux がユーザーとグループを識別する仕組みと、プロセスの資格情報がアクセス判断へ与える影響を学びます。"
meta_title: "ユーザーとグループ - ユーザー管理"
meta_description: "Linux の基本の重要な部分は、ユーザーとグループの管理を理解することです。このガイドでは、Linux のユーザーとグループ、root スーパーユーザー、および sudo コマンドを使用した権限昇格について説明します。初心者向けの最高の Linux チュートリアルレッスンの 1 つです。"
meta_keywords: "Linux ユーザーとグループ，Linux の基本，sudo, root ユーザー, UID, GID, ユーザー管理，最高の Linux チュートリアル，Linux 上級への最短ルート"
---

Linux はユーザーとグループの識別情報を使って、プロセスに身元を持たせ、ファイルシステムオブジェクトの所有者を記録し、アクセス制御を判断します。人が読める名前は管理者に便利ですが、カーネルが主に扱うのは数値の識別子とプロセス資格情報です。

## UID でユーザーを識別する

各アカウントには、ユーザー ID、つまり **UID** と呼ばれる数値があります。ユーザー名はシステムのアカウントデータベースを通じて UID に対応付けられます。ファイルには数値の所有者が保存され、ツールは通常、それに対応する名前を表示します。

現在のプロセスの識別情報は `id` で調べます。

```bash
$ id
uid=1000(alice) gid=1000(alice) groups=1000(alice),27(sudo)
```

値はシステムによって異なります。人がログインするアカウントには `/home/alice` のようなホームディレクトリがあるのが一般的ですが、別のパスを使う場合や、通常のホームを持たない場合もあります。サービスアカウントは、対話的ログインではなく、制限された識別情報でソフトウェアを実行するために存在することがよくあります。

:::single-choice{#users-uid-purpose} カーネルがユーザー識別情報を表すために主に使う識別子はどれですか？

::option[ホームディレクトリのパス名]{#users-home-path explanation="ホームパスはアカウント設定であり、異なる場合や存在しない場合があります。カーネルのユーザー識別子ではありません。"}
::option[数値の UID]{#users-numeric-uid .correct explanation="アカウントデータベースは名前を数値 UID に対応付け、UID はプロセス資格情報と所有権記録で使われます。"}
::option[端末ウィンドウの番号]{#users-terminal-number explanation="端末デバイスやセッションは、数値のユーザー識別情報とは別のものです。"}
:::

## グループでアクセスを整理する

グループには、グループ ID、つまり **GID** と呼ばれる数値があります。通常、アカウントには一つのプライマリグループがあり、補助グループにも所属できます。グループを使えば、アカウントごとに権限を割り当てず、複数ユーザーへまとめてアクセスを許可できます。

所属状況は次のように調べます。

```bash
$ id alice
$ groups alice
```

これらのコマンドは、設定または名前解決された識別情報を報告します。ディレクトリサービスやキャッシュが関与する場合があるため、`/etc/group` を直接読んでも、有効な所属関係をすべて確認できるとは限りません。

:::single-choice{#users-primary-supplementary-groups} 一つの Linux アカウントは通常、どのようにグループへ参加しますか？

::option[存続期間中、厳密に一つのグループだけに所属できる。]{#users-single-group explanation="Linux プロセスは、プライマリグループに加えて補助グループの一覧を持てます。"}
::option[読み取れるファイルを所有するすべてのグループに所属する。]{#users-readable-groups explanation="ファイルの読み取り可否は権限と資格情報に従うもので、自動的にグループ所属を作るわけではありません。"}
::option[一つのプライマリグループを持ち、補助グループにも所属できる。]{#users-group-memberships .correct explanation="プライマリ GID はアカウント記録の一部で、補助グループは追加のグループ識別情報を提供します。"}
:::

## プロセス資格情報を理解する

プロセスは、実 UID と実効 UID、実 GID と実効 GID、補助グループなどの資格情報を持ちます。多くの権限確認では、実効資格情報が中心となります。ユーザーが開始したプロセスは通常、親から資格情報を継承しますが、制御された仕組みで変更されることがあります。

これは「プロセスは常に、起動したユーザーとしてだけ動く」という説明より正確です。set-user-ID 実行ファイル、サービスマネージャー、コンテナ、名前空間、権限を変更するシステムコールによって、特定のコンテキストで見える、または有効な識別情報が変わる場合があります。

:::single-choice{#users-process-access-identity} カーネルがプロセスとファイル権限を照合するとき、一般に考慮する情報はどれですか？

::option[プロセスの実効 UID、実効 GID、補助グループ。]{#users-effective-credentials .correct explanation="通常の任意アクセス制御では、これらの資格情報を所有権と権限データに照合します。"}
::option[プロセスを起動した端末の配色テーマ。]{#users-terminal-theme explanation="表示設定はファイルシステムの権限確認には関与しません。"}
::option[アカウントのユーザー名の文字数。]{#users-username-length explanation="カーネルは数値の資格情報を扱い、ユーザー名の長さがアクセスを許可することはありません。"}
:::

## root の識別情報を認識する

伝統的に `root` と名付けられたアカウントの UID は0です。Linux の多くの権限機構は UID 0 を特別に扱い、広範な管理権限を与えます。現代の Linux は、ケーパビリティ、名前空間、強制アクセス制御、サービスの隔離によって権限を分割することもできるため、「あらゆるコンテキストで無制限の力を持つ」という説明は単純化しすぎです。

日常の作業には非特権アカウントを使います。管理権限があると、パスの誤り、信頼できないコマンド、侵害されたソフトウェアによる影響が大きくなります。

:::single-choice{#users-root-uid} 伝統的に root アカウントを識別する数値 UID はどれですか？

::option[`0`]{#users-uid-zero .correct explanation="Linux と Unix 系システムでは、伝統的に UID 0 をスーパーユーザー識別情報として予約します。"}
::option[`1000`]{#users-uid-thousand explanation="多くのディストリビューションは最初の通常ユーザーへ1000前後を割り当てますが、root の UID ではありません。"}
::option[`1`]{#users-uid-one explanation="UID 1 はシステムアカウントに割り当てられる場合があり、伝統的なスーパーユーザー識別情報ではありません。"}
:::

## ポリシーに基づいて sudo を使う

`sudo` は、呼び出したユーザーが対象ユーザーとしてコマンドを実行してよいか、設定済みポリシーへ問い合わせます。既定の対象は root であることが多いものの、ポリシーまたは `-u USER` で別のアカウントを選べます。認証プロンプトとログ記録も設定によって異なります。

現在のアカウントに許可されたコマンドは次のように一覧表示します。

```bash
$ sudo -l
```

管理コマンドは、作業に必要で、その影響を理解している場合だけ使ってください。権限エラーを消すためだけに `sudo` を使ったり、`/etc/shadow` のようなパスワードハッシュデータベースを気軽な練習として表示したりしてはいけません。

:::single-choice{#users-sudo-policy} `sudo` は要求されたコマンドを実行する前に何をしますか？

::option[要求された対象識別情報の使用が許可されるか、設定済みポリシーへ問い合わせる。]{#users-sudo-policy-check .correct explanation="`sudo` はポリシーに従って認可し、許可された場合に設定された対象の資格情報を確立します。"}
::option[すべてのローカルユーザーへ常に無制限の root アクセスを与える。]{#users-sudo-always-root explanation="認可はポリシーで制御され、拒否されたユーザーやコマンドに一律の root アクセスは与えられません。"}
::option[呼び出したアカウントの UID を恒久的に0へ変更する。]{#users-sudo-permanent-uid explanation="`sudo` は対象の資格情報でコマンドを実行しますが、呼び出し元アカウントの識別情報を恒久的に書き換えません。"}
:::

管理された環境でアカウントとグループの管理を練習するには、次のハンズオンラボを利用できます。

1. **[useradd、usermod、userdel で Linux ユーザーアカウントを管理する](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - 新規アカウントの作成と保護から変更、削除まで、ユーザー管理のライフサイクル全体を練習します。
2. **[groupadd、usermod、groupdel で Linux グループを管理する](https://labex.io/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** - 新規グループの作成、ユーザー所属の変更、グループの削除など、グループ管理の主要なコマンドラインユーティリティーを実践します。
3. **[Linux でユーザーアカウントと sudo 権限を設定する](https://labex.io/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** - 管理権限の付与を含め、Linux システムのセキュリティを高めるユーザーアカウントと `sudo` 権限の重要な管理方法を学びます。

## まとめ

Linux が識別情報を表し、管理コマンドを委任する仕組みを説明できるようになりました。

1. アカウントを UID、グループを GID で識別する。
2. プライマリグループと補助グループへの所属を区別する。
3. プロセス資格情報をアクセス確認に関連付ける。
4. UID 0 を伝統的な root 識別情報として認識する。
5. `sudo` をポリシーで制御される委任ツールとして扱う。
