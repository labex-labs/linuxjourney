---
lesson_id: "boot-process-bios"
course_id: "boot-system"
lang: "ja"
order_index: 2
title: "起動プロセス：BIOS"
description: "従来の BIOS と現代の UEFI ファームウェアが、次のブート段階を検出して承認する仕組みを学びます。"
meta_title: "起動プロセス：BIOS - システムの起動"
meta_description: "Linux 起動プロセスの最初のステップである BIOS について解説します。BIOS が MBR または GPT を介してブートローダーをどのように見つけるか、そして UEFI の役割を学びます。このガイドではシステム起動を説明し、設定のために BIOS を起動する方法についても触れます。"
meta_keywords: "Linux 起動プロセス，BIOS, MBR, UEFI, Linux での BIOS, BIOS Linux, BIOS を起動する方法，ブートローダー, システム起動"
---

ファームウェアは Linux カーネルより先に動作します。PC クラスのハードウェアでは、従来型 BIOS と UEFI が主要な二つのインターフェースです。両者は異なる方法でブート対象を検出するため、「BIOS がブートローダーを読み込む」という説明は一方の経路にしか当てはまりません。

## 従来型 BIOS のブート

初期のプラットフォーム初期化とブートデバイス選択を終えると、従来型 BIOS は通常、選択したディスクの先頭 512 バイトのセクターを読み取り、想定される署名があれば、そのブートコードへ制御を渡します。

MBR レイアウトでは、このセクターに小さなブートコード領域、四つのパーティションエントリ、署名が収まります。高機能なローダーには小さすぎるため、そのコードは多くの場合、ディスク上やファイルシステム内にある次の段階を探します。

GPT ディスクから BIOS ブートすることもできますが、protective MBR だけではローダーの後続段階を格納できません。GRUB は通常、GPT 上の小さな BIOS Boot Partition に組み込み用コアコードを置きます。正確な配置は、インストールしたローダーによって決まります。

:::single-choice{#boot-bios-legacy-first-sector}
従来型 BIOS は、選択したブートディスクから最初に何を読み込むのが一般的ですか？

::option[小さなブートコードを含む先頭ブートセクター。]{#boot-bios-boot-sector .correct explanation="従来のディスクブート経路では、ファームウェアは選択したディスクの先頭セクターにあるコードへ制御を渡します。"}
::option[Linux のルートファイルシステム全体。]{#boot-bios-entire-root explanation="第 1 段階のセクターは非常に小さく、カーネルやルートストレージは後続ソフトウェアが探します。"}
::option[`/etc` 以下にある全ユーザーサービス設定。]{#boot-bios-etc-config explanation="ファームウェアは、インストール済みシステムの完全なサービス設定を解析しません。"}
:::

## UEFI ブート

UEFI ファームウェアは EFI System Partition（ESP）上の定められたファイルシステムを理解し、EFI 実行ファイルを読み込めます。不揮発性変数に保存されたファームウェアのブートエントリは、通常、ディスク、パーティション、実行ファイルのパスを指定します。リムーバブルメディアや復旧時には、標準化されたフォールバックパスも利用できます。

ESP に格納されるのはブートアプリケーションと補助ファイルであり、「起動情報のすべて」ではありません。ブート設計によって、カーネルイメージ、initramfs、ローダー設定は ESP 上にも別の場所にも置けます。UEFI システムでは GPT が一般的ですが、ファームウェアインターフェースとパーティションテーブル方式は別々の層です。

:::single-choice{#boot-bios-uefi-esp}
UEFI は EFI System Partition から何を読み込むのが一般的ですか？

::option[ファームウェアのブートエントリで選択された EFI 実行ファイル。]{#boot-bios-efi-executable .correct explanation="UEFI のブート管理は、対応するシステムパーティション上の実行ファイルをファームウェアに指定します。"}
::option[任意の ext4 ホームディレクトリにある POSIX シェルスクリプト。]{#boot-bios-shell-script explanation="ファームウェアは、通常のユーザーシェルではなく、対応するブート経路から定められた形式の実行ファイルを読み込みます。"}
::option[ユーザーアカウントを含む MBR 拡張パーティション。]{#boot-bios-extended-users explanation="アカウントデータは、UEFI による実行ファイルの検出とは無関係です。"}
:::

## Secure Boot と信頼

Secure Boot を有効にすると、UEFI は登録されたプラットフォーム鍵とポリシーに従ってブートチェーンの署名を検証します。Linux ディストリビューションは、署名済み shim、ブートローダー、カーネル、カーネルモジュールのポリシーを使って、この信頼の連鎖を延長できます。

Secure Boot はディスクを暗号化せず、すべてのユーザー空間プログラムが安全だと証明するものでもありません。設定された信頼ポリシーの下で、未承認のプリブートコードが受け入れられるのを防ぐのに役立ちます。

:::single-choice{#boot-bios-secure-boot-purpose}
UEFI Secure Boot が主に強制するものは何ですか？

::option[全ディスク上の全ファイルの自動暗号化。]{#boot-bios-secure-encryption explanation="ディスクの機密性を守るには、別の暗号化システムが必要です。"}
::option[署名に基づくブートチェーン実行ファイルの承認。]{#boot-bios-secure-signatures .correct explanation="ファームウェアと後続の検証済みコンポーネントは、登録済みの鍵とポリシーに従ってコードを受け入れます。"}
::option[署名済みソフトウェアに脆弱性がないことの保証。]{#boot-bios-secure-no-vulnerabilities explanation="有効な署名が証明するのは承認と完全性であり、コードに欠陥がないことではありません。"}
:::

## ファームウェア設定を開く

ファームウェア設定を開くキーはメーカーやモデルごとに異なり、起動初期の Delete、Escape、ファンクションキーなどがよく使われます。無作為に設定を変えず、デバイスの文書を確認してください。一部の UEFI システムでは、OS からファームウェア設定へ再起動するよう要求することもできます。

Secure Boot、ストレージコントローラーのモード、TPM、仮想化、ブート順序を変える前に、既存の値と復旧キーを記録してください。ファームウェア設定の変更によって、暗号化ボリュームやインストール済み OS に一時的にアクセスできなくなることがあります。

:::single-choice{#boot-bios-setup-key}
ファームウェア設定を開く共通キーが存在しないのはなぜですか？

::option[Linux がブートのたびに新しいキーを無作為に割り当てるから。]{#boot-bios-random-key explanation="OS がファームウェア初期起動時のキーを無作為に決めるわけではありません。"}
::option[キーと入力タイミングをシステムメーカーが決めるから。]{#boot-bios-vendor-key .correct explanation="ファームウェアの操作方法はモデルごとに異なるため、正規のデバイス文書を確認する必要があります。"}
::option[ブートローダーを削除しないと設定を開けないから。]{#boot-bios-delete-loader explanation="ファームウェア設定を開く操作は、インストール済みブートファイルの破壊とは無関係です。"}
:::

## まとめ

これで、従来型 BIOS と UEFI で異なるブート検出モデルを区別できます。

1. 従来型 BIOS と先頭セクターのブートコード、後続ローダー段階を関連付ける。
2. UEFI ブートエントリと ESP 上の EFI 実行ファイルを関連付ける。
3. GPT、ファームウェアインターフェース、ブートローダー配置を別々の選択肢として扱う。
4. ファームウェアの信頼設定やストレージ設定は、復旧手段を用意してから変更する。
