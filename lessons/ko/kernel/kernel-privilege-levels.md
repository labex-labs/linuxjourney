---
lesson_id: "kernel-privilege-levels"
course_id: "kernel"
lang: "ko"
order_index: 2
title: "권한 수준"
description: "프로세서 권한이 사용자 실행과 신뢰되는 커널 실행을 분리하는 방법을 알아봅니다."
meta_title: "권한 수준 - 커널"
meta_description: "리눅스 권한 수준의 핵심 개념을 살펴봅니다. 커널 모드와 사용자 모드, 보호 링 및 시스템 호출을 통한 특권 하드웨어 접근의 차이를 설명합니다."
meta_keywords: "리눅스 권한 수준, 커널 모드, 사용자 모드, 보호 링, 시스템 호출, 특권 접근, 커널 권한, 리눅스 보안"
---

프로세서는 민감한 명령어와 메모리 접근을 제한하는 권한 모드를 제공합니다. 리눅스는 이 하드웨어 경계를 사용해 일반 애플리케이션의 오류가 커널 메모리를 직접 덮어쓰거나 장치를 재설정하지 못하게 합니다. 특권 실행으로의 전환은 커널이 제어합니다.

## 사용자 모드

일반 프로세스는 자체 가상 주소 공간 안에서 사용자 모드로 실행됩니다. 자유롭게 계산하고 커널이 허용한 메모리 매핑에 접근할 수 있으며 그 범위는 클 수 있습니다. 사용자 모드가 “아주 적은 메모리만 사용한다”는 뜻은 아닙니다. 임의의 물리 메모리, 다른 프로세스의 비공개 매핑 또는 특권 프로세서 제어에는 직접 접근할 수 없습니다.

페이지 테이블과 보호 비트가 메모리 접근을 적용합니다. 스레드가 유효하지 않거나 허용되지 않은 주소를 참조하면 프로세서가 커널로 트랩하고, 커널은 유효한 페이지 폴트를 해결하거나 `SIGSEGV` 같은 신호를 전달할 수 있습니다.

:::single-choice{#kernel-privilege-user-mode-memory}
사용자 모드 프로세스가 일반적으로 직접 접근할 수 있는 메모리는 무엇입니까?

::option[모든 물리 RAM 주소와 전체 커널 메모리입니다.]{#kernel-privilege-all-physical explanation="권한과 가상 메모리 보호가 그러한 접근을 막습니다."}
::option[프로세스 시작 시 선택된 고정된 한 바이트뿐입니다.]{#kernel-privilege-one-byte explanation="프로세스는 비특권 상태에서도 여러 매핑 영역을 가질 수 있습니다."}
::option[자체 가상 주소 공간에서 허용된 매핑입니다.]{#kernel-privilege-own-mappings .correct explanation="하드웨어 페이지 보호는 적절한 접근 권한으로 설정된 매핑으로 프로세스를 제한합니다."}
:::

## 커널 모드

커널 모드에서는 메모리 관리, 스케줄링, 인터럽트 처리 및 드라이버에 필요한 특권 명령어를 실행하고 보호된 커널 매핑에 접근할 수 있습니다. x86에서 이 리눅스 구분은 일반적으로 커널의 링 0과 사용자 프로세스의 링 3으로 설명됩니다. 리눅스는 보통 일반 프로세스 격리에 링 1과 2를 사용하지 않습니다.

다른 아키텍처는 예외 수준 같은 서로 다른 이름과 메커니즘을 사용합니다. 가상화는 단순한 두 링 그림으로 표현할 수 없는 하이퍼바이저 및 게스트 관계를 추가합니다. 핵심 개념은 x86 링 번호 자체가 아니라 제어된 권한입니다.

:::single-choice{#kernel-privilege-x86-kernel-ring}
리눅스 커널은 일반적으로 어느 x86 보호 링에서 실행됩니까?

::option[링 3입니다.]{#kernel-privilege-ring-three explanation="링 3은 일반적인 사용자 모드 권한 수준입니다."}
::option[링 0입니다.]{#kernel-privilege-ring-zero .correct explanation="커널은 전통적인 x86에서 가장 높은 권한을 가진 링을 사용합니다."}
::option[링 7입니다.]{#kernel-privilege-ring-seven explanation="전통적인 x86 보호 링은 0부터 3까지입니다."}
:::

## 제어된 전환

다음과 같은 여러 이벤트가 커널 진입점으로 제어권을 넘깁니다.

- 시스템 호출 명령어가 커널 서비스를 요청
- 예외가 페이지 폴트나 잘못된 명령어 같은 상태를 보고
- 하드웨어 인터럽트가 외부 이벤트를 보고

프로세서는 실행 컨텍스트를 저장하고 설정된 진입 메커니즘에 따라 권한을 바꾼 뒤 신뢰되는 커널 코드를 시작합니다. 커널은 요청과 상태를 검증하고 작업을 수행하거나 거부한 다음 적절할 때 사용자 모드로 돌아갑니다.

애플리케이션이 일시적으로 커널 코드가 되는 것은 아닙니다. CPU가 커널이 제어하는 스택과 매핑을 사용해 해당 스레드를 대신하여 커널 핸들러를 실행합니다.

:::single-choice{#kernel-privilege-system-call-transition}
시스템 호출 전환 중에는 어떤 일이 일어납니까?

::option[애플리케이션의 사용자 코드가 제한 없는 링 0 실행 권한을 받습니다.]{#kernel-privilege-user-ring-zero explanation="제어된 진입 후에는 신뢰되는 커널 코드만 실행됩니다."}
::option[프로세스의 UID가 영구적으로 0으로 바뀝니다.]{#kernel-privilege-uid-zero explanation="프로세서 모드 전환은 사용자 자격 증명을 다시 쓰지 않습니다."}
::option[요청을 검증하는 정의된 커널 핸들러로 제어권이 들어갑니다.]{#kernel-privilege-kernel-handler .correct explanation="프로세서는 반환을 위해 사용자 컨텍스트를 보존하면서 설정된 진입 경로를 통해 모드를 바꿉니다."}
:::

## CPU 권한과 사용자 식별 정보는 다름

리눅스 사용자 `root`로 실행되는 애플리케이션도 일반적으로 사용자 모드에서 실행됩니다. UID 0은 커널 권한 부여 검사에 영향을 주지만 명령어가 커널 메모리에 직접 접근하게 하지는 않습니다. 반대로 어떤 사용자의 시스템 호출 때문에 실행되었는지와 관계없이 커널 코드는 특권 모드에서 실행됩니다.

capability, 네임스페이스, seccomp, 보안 모듈 및 cgroup은 프로세스가 요청할 수 있는 작업을 추가로 제한합니다. 이러한 계층적 정책은 하드웨어 사용자/커널 모드 경계와 별개입니다.

:::single-choice{#kernel-privilege-root-distinction}
root 식별 정보와 커널 모드를 올바르게 비교한 설명은 무엇입니까?

::option[root는 사용자 공간 자격 증명이고 커널 모드는 프로세서 실행 권한입니다.]{#kernel-privilege-credential-versus-mode .correct explanation="root 프로세스는 사용자 모드에서 권한 있는 요청을 보내고 신뢰되는 커널 코드가 특권 실행을 수행합니다."}
::option[root가 소유한 모든 명령어는 로드 가능한 커널 코드로 실행됩니다.]{#kernel-privilege-root-kernel-code explanation="UID 소유권은 실행 파일을 커널 모듈로 바꾸지 않습니다."}
::option[커널 모드는 `/etc/passwd`에 저장된 또 다른 사용자 이름입니다.]{#kernel-privilege-kernel-username explanation="프로세서 모드는 로그인 계정이 아니라 하드웨어 상태입니다."}
:::

## 경계가 중요한 이유

이 경계는 일반적인 버그의 피해를 제한하고 접근 검사를 수행할 지점을 제공하지만 커널 취약점과 악성 모듈은 이를 무너뜨릴 수 있습니다. 신뢰할 수 있는 경로로 커널과 펌웨어를 업데이트하고, 특권 코드를 최소화하며, 신뢰할 수 없는 모듈을 불러오지 마십시오.

추측 실행 문제와 부채널도 하드웨어 격리에 지속적인 완화 조치가 필요함을 보여 줍니다. “서로 다른 링”은 기반일 뿐 완전한 보안의 증명이 아닙니다.

:::single-choice{#kernel-privilege-boundary-limit}
사용자/커널 모드 분리가 완전한 시스템 보안을 보장합니까?

::option[그렇습니다. 커널 취약점은 사용자 프로세스에 영향을 줄 수 없습니다.]{#kernel-privilege-no-kernel-vulns explanation="커널 취약점은 전체 시스템을 손상시킬 수 있습니다."}
::option[아닙니다. 특권 코드 결함과 부채널은 의도한 경계를 여전히 넘을 수 있습니다.]{#kernel-privilege-not-complete .correct explanation="모드 구분은 공격 표면을 줄이지만 올바른 커널 코드와 추가 완화 조치를 함께 사용해야 합니다."}
::option[그렇습니다. 하드웨어 모드가 접근 제어 정책의 필요성을 없앱니다.]{#kernel-privilege-no-policy explanation="인증된 리소스 공유에는 자격 증명과 보안 정책이 계속 필수적입니다."}
:::

## 요약

이제 하드웨어 실행 권한과 리눅스 계정 권한을 구분할 수 있습니다.

1. 사용자 모드를 보호된 가상 주소 공간과 연결합니다.
2. 커널 모드를 특권 명령어 및 매핑과 연결합니다.
3. 시스템 호출, 예외 및 인터럽트를 제어된 진입으로 취급합니다.
4. UID 0 권한 부여와 링 0 실행을 구분합니다.
5. 권한 모드를 더 넓은 보안 설계의 한 계층으로 이해합니다.
