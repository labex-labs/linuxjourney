---
lesson_id: "what-is-a-router"
course_id: "routing"
lang: "ko"
order_index: 1
title: "라우터란?"
description: "라우터가 다음 홉을 선택하고 네트워크 사이에서 IP 패킷을 전달하는 방법을 알아봅니다."
meta_title: "라우터란? - 라우팅"
meta_description: "네트워킹에서 라우터가 무엇인지 알아봅니다. 라우팅, 패킷 교환, 홉 및 라우팅 테이블을 사용한 네트워크 간 데이터 전달을 설명합니다."
meta_keywords: "라우터, 네트워킹, 라우팅, 홉, 패킷 교환, 리눅스 네트워킹"
---

라우터는 네트워크 계층 도메인을 연결하고 그 사이에서 IP 패킷을 전달합니다. 전달이 활성화되고 인터페이스, 경로, 이웃 탐색 및 필터링 정책이 적절히 설정되면 리눅스 호스트도 라우터로 작동할 수 있습니다.

## 라우팅과 전달

라우팅은 연결 가능한 접두사 정보를 만들거나 선택합니다. 전달은 이 정보를 각 패킷에 적용합니다. 목적지를 조사하고, 적격 경로와 다음 홉을 선택하고, 홉 제한을 줄인 뒤 출력 인터페이스를 통해 전송합니다.

이 둘은 각각 제어 평면과 데이터 평면의 관심사입니다. 경로가 있어도 방화벽 정책이 전달을 막을 수 있고, 전달 인터페이스가 활성 상태여도 유효한 경로가 없을 수 있습니다.

:::single-choice{#router-forwarding-role}
패킷 전달은 무엇을 합니까?

::option[라우팅 정보를 적용해 패킷을 다음 홉 방향으로 보냅니다.]{#router-apply-route .correct explanation="전달은 선택된 경로와 정책을 바탕으로 패킷마다 수행하는 동작입니다."}
::option[모든 목적지에 영구적인 애플리케이션 로그인을 만듭니다.]{#router-create-login explanation="라우팅은 원격 애플리케이션 계정을 관리하지 않습니다."}
::option[경로가 없으면 모든 인터페이스에 패킷을 복사합니다.]{#router-flood-no-route explanation="일반적인 IP 전달은 Ethernet 방식의 플러딩을 대체 수단으로 사용하지 않고 라우팅할 수 없는 패킷을 버립니다."}
:::

## 라우팅 테이블과 기본 경로

경로는 목적지 접두사를 출력 인터페이스, 다음 홉, 메트릭, 출발지 선호도 또는 기타 속성과 연결합니다. 최장 접두사 일치는 더 구체적인 적격 경로를 우선합니다. IPv4 `/0` 또는 IPv6 `::/0` 기본 경로는 가장 덜 구체적인 일치이며 더 구체적인 경로가 없을 때만 사용됩니다.

적격 경로가 없으면 라우터는 패킷을 버리고 ICMP 도달 불가 메시지를 생성할 수 있습니다. 기본 경로는 선택 사항이며 공용 인터넷을 직접 가리킬 필요도 없습니다.

:::single-choice{#router-default-route}
기본 경로는 언제 선택됩니까?

::option[목적지별 접두사를 확인하기 전에 선택됩니다.]{#router-default-first explanation="더 구체적인 적격 접두사가 우선합니다."}
::option[패킷이 Ethernet 브로드캐스트일 때만 선택됩니다.]{#router-default-broadcast explanation="IP 경로 선택은 네트워크 계층 목적지를 기반으로 합니다."}
::option[더 구체적으로 일치하는 적격 경로가 없을 때 선택됩니다.]{#router-default-fallback .correct explanation="길이가 0인 접두사는 가장 덜 구체적인 경로입니다."}
:::

## 로컬 트래픽과 라우팅되는 트래픽

같은 링크상 서브넷의 두 호스트는 일반적으로 IP 패킷을 라우터로 보내지 않고 프레임을 교환합니다. 경로 선택에서 라우터를 다음 홉으로 고르거나 토폴로지와 정책이 의도적으로 라우팅 경유를 강제할 때 라우터가 관여합니다.

가정용 “라우터”는 흔히 IP 라우터, Ethernet 스위치, Wi-Fi 접근 지점, DHCP 서비스, NAT 및 방화벽을 결합합니다. 각 기능을 별도로 진단해야 합니다.

:::single-choice{#router-same-subnet-path}
링크상에 있는 두 호스트 사이의 트래픽은 반드시 기본 라우터를 통과해야 합니까?

::option[예. 모든 패킷이 WAN 포트에 도달해야 하기 때문입니다.]{#router-always-wan explanation="로컬 링크상 전송은 링크를 통해 직접 이뤄질 수 있습니다."}
::option[예. 두 호스트에 공인 주소가 있지 않은 한 그렇습니다.]{#router-public-required explanation="공인 또는 사설 범위는 기본 링크상 전달 여부를 결정하지 않습니다."}
::option[아니요. 송신자는 로컬 링크에서 목적지를 직접 지정할 수 있습니다.]{#router-direct-on-link .correct explanation="라우팅 테이블은 연결된 접두사를 링크상으로 식별합니다."}
:::

## 홉과 루프 방지

라우팅 홉은 네트워크 계층 전달 단계입니다. IPv4 TTL과 IPv6 Hop Limit은 라우터마다 줄어들어 루프를 제한합니다. 홉 수는 완전한 거리나 품질 지표가 아닙니다. 링크마다 대역폭, 지연 시간, 손실, 정책 및 혼잡이 다릅니다.

:::single-choice{#router-hop-count-limit}
더 적은 홉 수가 보장하지 못하는 것은 무엇입니까?

::option[라우팅 단계가 하나 이상 존재한다는 사실입니다.]{#router-hop-exists explanation="양의 홉 수는 라우팅 경유를 직접 나타냅니다."}
::option[더 빠르거나 더 나은 애플리케이션 경로입니다.]{#router-hop-not-quality .correct explanation="라우터가 적어도 더 느리거나 혼잡하거나 정책으로 제한된 링크를 통과할 수 있습니다."}
::option[홉 제한 필드가 유한하다는 사실입니다.]{#router-hop-limit-finite explanation="이 필드는 프로토콜 설계상 유한합니다."}
:::

## 요약

이제 라우터의 경로 선택과 전달 동작을 구분할 수 있습니다.

1. 라우터를 IP 네트워크 사이에서 전달하는 장치로 정의합니다.
2. 제어 평면 라우팅과 데이터 평면 전달을 구분합니다.
3. 기본 경로를 가장 덜 구체적인 대체 경로로 다룹니다.
4. 홉 수만으로 경로 품질을 측정할 수 없음을 이해합니다.
