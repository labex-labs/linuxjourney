---
lesson_id: "kernel-overview"
course_id: "kernel"
lang: "ru"
order_index: 1
title: "Обзор ядра"
description: "Узнайте, как ядро Linux посредничает между оборудованием, ресурсами, изоляцией и запросами пользовательского пространства."
meta_title: "Обзор ядра - Ядро"
meta_description: "Познакомьтесь с ядром Linux и его центральной ролью в управлении оборудованием и пользовательским пространством."
meta_keywords: "ядро Linux, операционная система, оборудование, пользовательское пространство, обзор ядра, управление ресурсами"
---

Linux — ядро операционной системы: привилегированное ПО, управляющее процессорами, памятью, устройствами, процессами и общими абстракциями ресурсов. Полная система Linux также включает библиотеки, утилиты, службы, оболочки, графические программы и политику дистрибутива в пользовательском пространстве.

## Аппаратные ресурсы

Процессоры исполняют инструкции, память хранит активное состояние, а контроллеры соединяют хранилища, сети, дисплеи, устройства ввода и другую периферию. Оборудование предоставляет специфичные архитектуре и устройству механизмы, а не единый безопасный интерфейс каждому приложению.

Ядро инициализирует ресурсы и управляет ими через архитектурный код и драйверы устройств. Оно обрабатывает прерывания, координацию DMA, таймеры и события управления питанием, одновременно обеспечивая границы доступа между нагрузками.

:::single-choice{#kernel-overview-hardware-manager}
Какой уровень обычно координирует драйверы устройств и аппаратные прерывания в Linux?

::option[Файл истории оболочки каждого пользователя.]{#kernel-overview-shell-history explanation="История записывает команды и не управляет выполнением оборудования."}
::option[Индекс репозитория пакетов.]{#kernel-overview-repository-index explanation="Метаданные репозитория описывают пакеты, а не живые аппаратные события."}
::option[Ядро.]{#kernel-overview-kernel-layer .correct explanation="Привилегированный код ядра связывает аппаратные события и операции драйверов с контролируемыми системными интерфейсами."}
:::

## Обязанности ядра

Основные обязанности:

- планировать выполняемые threads на CPU;
- создавать и изолировать виртуальные адресные пространства;
- обеспечивать учётные данные процессов, разрешения и политику безопасности;
- предоставлять файловые системы, сеть, IPC и интерфейсы устройств;
- обрабатывать сигналы, таймеры и жизненный цикл процессов;
- выделять, учитывать и освобождать ресурсы.

Linux обычно называют монолитным ядром, поскольку основные службы и многие драйверы выполняются в одном привилегированном адресном пространстве. При этом оно модульно: поддерживаемые компоненты можно загружать и выгружать как модули. Ошибка в привилегированном коде способна скомпрометировать всю систему, поэтому обновления ядра и происхождение модулей критичны для безопасности.

:::single-choice{#kernel-overview-scheduler-role}
Чем управляет планировщик ядра?

::option[Какую страницу документации пользователь прочитает дальше.]{#kernel-overview-documentation explanation="Навигация обучения не относится к планированию ядра."}
::option[Какие готовые threads получают время выполнения CPU.]{#kernel-overview-thread-scheduling .correct explanation="Планировщик выбирает контексты выполнения по политике, приоритету, affinity и доступности CPU."}
::option[Какому ключу подписи репозитория должен доверять администратор.]{#kernel-overview-repository-key explanation="Настройка доверия относится к политике управления пакетами."}
:::

## Пользовательское пространство

В пользовательском пространстве работают обычные процессы: init и службы, командные инструменты, языковые среды, базы данных, оболочки и настольные приложения. Аппаратные привилегии не дают им напрямую выполнять многие чувствительные инструкции или обращаться к произвольной памяти ядра.

Процессы запрашивают работу ядра через системные вызовы и взаимодействуют с такими интерфейсами, как файловые дескрипторы, сокеты, узлы устройств, procfs, sysfs, netlink и отображения памяти. Библиотеки часто оборачивают их в высокоуровневые API.

Root в пользовательском пространстве имеет широкую авторизацию по политике, но обычно всё равно выполняется в пользовательском режиме процессора. Идентичность пользователя и режим привилегий CPU — разные понятия.

:::single-choice{#kernel-overview-root-user-mode}
Выполняет ли обычное приложение root все свои инструкции в режиме ядра?

::option[Да, UID 0 навсегда переводит каждую инструкцию в ring 0.]{#kernel-overview-root-ring-zero explanation="Обычный процесс root остаётся процессом пользовательского пространства."}
::option[Да, приложения root автоматически становятся загружаемыми модулями ядра.]{#kernel-overview-root-module explanation="UID владельца не преобразует пользовательскую программу в код ядра."}
::option[Нет, обычно оно работает в пользовательском режиме и входит в ядро через контролируемые интерфейсы.]{#kernel-overview-root-userspace .correct explanation="Учётные данные root влияют на авторизацию, а режим процессора меняется только для входа и выполнения ядра."}
:::

## Границы и абстракции

Ядро представляет виртуальные процессы, файлы, сокеты и адресные пространства вместо прямого раскрытия физического оборудования. Эти абстракции поддерживают изоляцию и переносимость, но сами по себе не идеальны как границы безопасности. Namespaces, cgroups, capabilities, модули безопасности, seccomp и виртуализация добавляют специализированный контроль.

При диагностике спрашивайте, какой уровень отвечает за поведение: приложение, библиотека, системный вызов, файловая система, драйвер, подсистема ядра, прошивка или оборудование. Доказательства неверного уровня могут привести к неправильному исправлению.

:::single-choice{#kernel-overview-system-call-boundary}
Что такое системный вызов?

::option[Контролируемый запрос пользовательского пространства к службе ядра.]{#kernel-overview-controlled-request .correct explanation="Процессор входит в режим ядра через определённый интерфейс, где ядро проверяет и выполняет операцию."}
::option[Прямая команда, обходящая все проверки доступа.]{#kernel-overview-bypass-checks explanation="Именно при системных вызовах выполняются многие проверки и авторизация."}
::option[Архив пакета, содержащий драйвер устройства.]{#kernel-overview-package-archive explanation="Пакеты могут доставлять ПО, но syscall — runtime-интерфейс выполнения."}
:::

Используйте лабораторную работу [Управление модулями ядра в Linux](https://labex.io/ru/labs/comptia-manage-kernel-modules-in-linux-590865), чтобы наблюдать модульную часть ядра в контролируемой среде.

## Итоги

Теперь вы можете поместить ядро между физическими ресурсами и изолированными процессами пользовательского пространства.

1. Связывать драйверы и архитектурный код с управлением оборудованием.
2. Определять обязанности планирования, памяти, безопасности, файловых систем и сети.
3. Различать учётные данные root и режим ядра процессора.
4. Находить взаимодействие пользователя и ядра в контролируемых runtime-интерфейсах.
