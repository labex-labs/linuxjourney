---
lesson_id: "etc-shadow-file"
course_id: "user-management"
lang: "ru"
order_index: 4
title: "/etc/shadow"
description: "Узнайте, как локальные записи shadow представляют хеши паролей и политику сроков, не раскрывая чувствительные данные."
meta_title: "/etc/shadow — управление пользователями"
meta_description: "Изучите защищённый файл /etc/shadow в Linux, структуру его девяти полей, хеши паролей и сведения о сроках действия."
meta_keywords: "/etc/shadow, файл /etc/shadow в Linux, аутентификация пользователей, безопасность паролей, хеш пароля, системное администрирование Linux"
---

`/etc/shadow` хранит защищённые локальные хеши паролей и поля сроков их действия. Отделение этих значений от обычно доступной для чтения базы `/etc/passwd` снижает риск офлайн-подбора паролей.

## Защита данных shadow

Пароли не хранятся в обратимо «зашифрованном» виде для последующего отображения. Локальная запись обычно содержит односторонний хеш пароля, закодированный с идентификатором алгоритма, солью и параметрами. Получив хеши, атакующий может проверять варианты пароля без подключения к системе, поэтому база должна оставаться закрытой.

Точные владелец и права различаются, но доступ обычно ограничен root и узко авторизованными системными компонентами. Не выводите, не копируйте, не журналируйте и не передавайте содержимое shadow лишь для проверки состояния учётной записи.

:::single-choice{#shadow-restricted-reason} Почему локальные данные shadow обычно закрыты от общего чтения?

::option[Файл содержит текущий пароль каждого пользователя в открытом виде.]{#shadow-plaintext-passwords explanation="Корректные записи shadow хранят односторонние хеши или специальные маркеры, а не восстанавливаемые открытые пароли."}
::option[Раскрытые хеши паролей можно атаковать офлайн.]{#shadow-offline-guessing .correct explanation="Атакующий может проверять варианты по украденным хешам без взаимодействия со службой входа."}
::option[Чтение автоматически изменяет все даты истечения паролей.]{#shadow-read-changes explanation="Само чтение не обновляет поля политики; риск состоит в раскрытии чувствительного материала аутентификации."}
:::

## Формат из девяти полей

Локальная запись shadow содержит девять полей, разделённых двоеточиями. Схематическая запись с намеренно опущенным хешем:

```text
alice:<password-field>:20000:0:90:7:14:20500:
```

Поля:

1. **Имя входа**.
2. **Хеш пароля или специальный маркер пароля**.
3. **Последнее изменение пароля** в днях с 1970-01-01; значение `0` в типичных инструментах требует смены при следующем входе по паролю.
4. **Минимальный срок пароля** в днях.
5. **Максимальный срок пароля** в днях.
6. **Период предупреждения** до истечения пароля в днях.
7. **Период неактивности** после истечения пароля в днях.
8. **Дата истечения учётной записи** в днях с 1970-01-01.
9. **Зарезервированное поле**.

Пустые поля и специальные числовые значения имеют определённый смысл, который зависит от поля и инструмента. Используйте команды управления учётными записями, а не редактируйте значения на глаз.

:::single-choice{#shadow-account-expiration-field} Какое поле shadow хранит дату истечения учётной записи в днях с 1970-01-01?

::option[Поле 3]{#shadow-field-three explanation="Поле 3 содержит дату последнего изменения пароля, а не истечение учётной записи."}
::option[Поле 8]{#shadow-field-eight .correct explanation="Восьмое поле — абсолютное число дня истечения учётной записи."}
::option[Поле 5]{#shadow-field-five explanation="Поле 5 хранит максимальный срок пароля."}
:::

## Осторожная интерпретация поля пароля

Допустимый хеш в поле 2 поддерживает локальную проверку пароля Unix. Значение, начинающееся с `!`, обычно блокирует этот хеш, а `*` или другой недопустимый маркер не позволяет успешно проверить пароль через данное поле. Пустое значение опасно для безопасности и в зависимости от политики PAM может разрешать поведение без пароля.

Маркеры описывают локальный пароль, а не каждый возможный способ аутентификации. Открытые ключи SSH, сертификаты, токены и учётные данные приложений могут оставаться доступными, если не ограничены отдельно. Истечение учётной записи в поле 8 также отличается от блокировки пароля.

:::single-choice{#shadow-password-lock-scope} Какой безопасный вывод можно сделать из поля пароля shadow, начинающегося с `!`?

::option[Сохранённый хеш Unix-пароля сделан непригодным для обычной проверки пароля.]{#shadow-password-locked .correct explanation="Префикс `!` не позволяет хешу совпасть с введённым паролем через путь shadow."}
::option[Отключены все возможные способы входа в учётную запись.]{#shadow-all-login-disabled explanation="Другие методы аутентификации могут быть независимыми, поэтому один маркер не доказывает полную блокировку."}
::option[Учётная запись удалена из всех баз идентичностей.]{#shadow-account-deleted explanation="Запись shadow всё ещё существует, а удаление — отдельная операция управления учётной записью."}
:::

## Различие дат пароля и учётной записи

Поля 3–7 относятся к срокам пароля: когда он изменён, когда разрешено следующее изменение, когда он истекает, когда начинаются предупреждения и сколько после истечения остаётся доступным вход по паролю. Поле 8 прекращает действие учётной записи в абсолютный день независимо от возраста пароля.

Например, максимальный срок пароля 90 дней — не то же самое, что дата истечения учётной записи. Первый перемещается относительно последней смены пароля, а вторая остаётся фиксированной, пока администратор её не изменит.

:::single-choice{#shadow-max-age-versus-expire} Чем различаются поля shadow 5 и 8?

::option[Поле 5 хранит имя пользователя, а поле 8 — оболочку входа.]{#shadow-username-shell explanation="Имя находится в поле 1, а оболочка записана в `/etc/passwd`, не в shadow."}
::option[Поле 5 хранит хеш пароля, а поле 8 — его соль.]{#shadow-hash-salt explanation="Кодировка хеша находится в поле 2, а поля сроков не хранят соль отдельно."}
::option[Поле 5 — максимальный срок пароля, поле 8 — абсолютная дата истечения учётной записи.]{#shadow-password-vs-account-expiry .correct explanation="Срок пароля отсчитывается от последней смены, а истечение учётной записи хранится как абсолютное число дней."}
:::

## Проверка и изменение политики инструментами

Администраторы должны запрашивать только необходимые для задачи сведения:

```bash
$ sudo passwd -S alice
$ sudo chage -l alice
```

`passwd -S` кратко сообщает состояние локального пароля, а `chage -l` выводит сведения о сроках в читаемом виде. Форматы и требования авторизации могут различаться между дистрибутивами.

Для изменений используйте `passwd`, `chage`, `usermod` и связанные инструменты. Если ручной ремонт локальной базы shadow неизбежен, `vipw -s` обеспечивает блокировку; проверяйте базы командой `pwck`. Перед удалёнными изменениями аутентификации сохраняйте сеанс восстановления.

:::single-choice{#shadow-list-aging-policy} Какая команда предназначена для вывода читаемых сведений о сроках пароля локальной учётной записи `alice`?

::option[`cat /etc/shadow`]{#shadow-cat-entire-file explanation="Эта команда раскрывает все локальные записи shadow и больше чувствительных сведений, чем требуется."}
::option[`passwd -d alice`]{#shadow-passwd-delete explanation="Операция `-d` удаляет хеш пароля и является изменением состояния с последствиями для безопасности, а не просмотром."}
::option[`chage -l alice`]{#shadow-chage-list .correct explanation="Параметр `-l` просит `chage` показать поля сроков пароля в читаемом виде."}
:::

PAM и NSS могут подключать источники аутентификации и идентичностей помимо локальных shadow-файлов. Поэтому системная учётная запись может не иметь локальной записи shadow либо использовать дополнительные службы входа.

Чтобы попрактиковаться с состоянием учётных записей и политикой сроков в контролируемой среде, выполните лабораторные работы:

1. **[Управление учётными записями Linux с useradd, usermod и userdel](https://labex.io/ru/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** — отработайте создание, защиту, изменение и удаление учётных записей с помощью `useradd`, `passwd` и других инструментов.
2. **[Настройка учётных записей и привилегий sudo в Linux](https://labex.io/ru/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** — изучите управление пользователями, политики паролей и защиту учётных записей.

## Итоги

Теперь вы можете интерпретировать политику shadow, не раскрывая всю базу паролей.

1. Считайте хеши паролей закрытым материалом аутентификации.
2. Читайте девять полей shadow по их назначению.
3. Отличайте блокировку пароля от отключения всех способов входа.
4. Отделяйте сроки пароля от абсолютного истечения учётной записи.
5. Проверяйте и изменяйте политику точечными инструментами учётных записей.
