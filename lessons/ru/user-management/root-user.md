---
lesson_id: "root-user"
course_id: "user-management"
lang: "ru"
order_index: 2
title: "root"
description: "Узнайте, как su, sudo и политика sudoers предоставляют контролируемый доступ к привилегированным идентичностям."
meta_title: "root — управление пользователями"
meta_description: "Изучите роль root в Linux, различия su и sudo для получения привилегий и управление доступом через /etc/sudoers."
meta_keywords: "пользователь root в Linux, su, sudo, sudoers, visudo, суперпользователь, управление пользователями, права Linux"
---

Учётная запись с традиционным именем `root` имеет UID 0 и широкие полномочия в своём контексте безопасности. Для повседневной работы используйте непривилегированную учётную запись и повышайте права только для конкретной понятной административной задачи.

## Запуск оболочки другого пользователя с помощью su

`su`, то есть substitute user, запускает оболочку или команду с идентичностью другой учётной записи. Если имя не указано, целью по умолчанию является root:

```bash
$ su
```

Аутентификация управляется PAM и локальной политикой. Система может запросить пароль целевой учётной записи, ограничить круг пользователей `su` или оставить пароль root заблокированным. Не считайте знание пароля единственным условием.

Обычный `su` меняет идентичность, сохраняя бо́льшую часть текущего окружения. `su - USER`, также записываемый как `su --login USER`, запускает оболочку входа и создаёт окружение, более близкое к новому входу целевой учётной записи:

```bash
$ su - operator
```

Завершите вложенную оболочку, когда работа от имени цели закончена.

:::single-choice{#root-su-login-shell} Какая команда запрашивает оболочку входа от имени пользователя `operator`?

::option[`su - operator`]{#root-su-login-operator .correct explanation="Дефис запрашивает поведение оболочки входа и окружение, ориентированное на `operator`."}
::option[`su operator`]{#root-su-preserve-environment explanation="Эта команда меняет идентичность, но не запрашивает полную инициализацию оболочки входа, описанную здесь."}
::option[`sudo -l operator`]{#root-sudo-list-operator explanation="`sudo -l` перечисляет разрешённые политикой команды и не запускает требуемую оболочку входа."}
:::

## Запуск отдельной команды через sudo

`sudo COMMAND` запрашивает разрешение политики выполнить одну команду от имени целевого пользователя, которым по умолчанию обычно является root. Для другой цели используйте `-u USER`:

```bash
$ sudo -u postgres id
```

Это не означает, что запрос будет разрешён. Политика sudo учитывает вызывающего пользователя, узел, целевую идентичность, команду и другие условия. В зависимости от конфигурации аутентификация может использовать пароль вызывающего пользователя, другой механизм или не показывать запрос.

Когда возможно, предпочитайте одну узко ограниченную административную команду длительной привилегированной оболочке. Меньшая область снижает вероятность случайного выполнения последующих команд с повышенными правами.

:::single-choice{#root-sudo-target-user} Что запрашивает `sudo -u postgres id`?

::option[Навсегда переименовать текущую учётную запись в `postgres`.]{#root-sudo-rename explanation="`sudo` запускает команду с целевыми учётными данными и не переименовывает записи учётных записей."}
::option[Выполнить `id` с пользователем `postgres` в качестве цели, если политика разрешает.]{#root-sudo-postgres-id .correct explanation="Параметр `-u` выбирает целевую идентичность, а политика sudoers решает, разрешён ли запрос."}
::option[Перечислить всех пользователей с UID больше текущего.]{#root-sudo-list-uids explanation="Команда `id` сообщает идентичность своего процесса; этот синтаксис не перечисляет UID учётных записей."}
:::

## Отказ от постоянных привилегированных оболочек

Команды `su -`, `sudo -s` и `sudo -i` могут создать привилегированную оболочку, если политика разрешает. Каждая последующая команда в ней способна иметь повышенное влияние до выхода. Ошибки в путях, непроверенные сценарии и подстановки оболочки становятся опаснее.

Поведение аудита зависит от конфигурации. `sudo` обычно записывает вызовы, но одна зарегистрированная команда запуска оболочки не обязательно создаёт полный журнал каждой команды внутри неё. История оболочки, системный аудит и журналирование ввода-вывода sudo — отдельные механизмы со своими политиками.

:::single-choice{#root-persistent-shell-risk} Почему длительно работающая оболочка root опаснее повышения прав для одной понятной команды?

::option[Оболочки root автоматически удаляют каждую команду из всех систем аудита.]{#root-shell-no-audit explanation="Журналирование зависит от конфигурации; утверждение об автоматическом удалении всех записей неверно."}
::option[Оболочка запрещает пути файловой системы длиннее одного компонента.]{#root-shell-path-limit explanation="Привилегии не вводят такого ограничения путей; риск связан с полномочиями обычных операций."}
::option[Последующие команды сохраняют повышенное влияние до выхода из оболочки.]{#root-shell-elevated-scope .correct explanation="Постоянная привилегированная идентичность расширяет период, когда опечатка или ненадёжная команда может изменить защищённые ресурсы."}
:::

## Проверка авторизации sudo

Выполните `sudo -l`, чтобы вывести запросы, разрешённые текущей учётной записи активной политикой:

```bash
$ sudo -l
```

Проверяйте пути команд, разрешённых целевых пользователей и ограничения аргументов. Правило с широкой формулировкой не следует считать разрешением выполнять несвязанную работу.

:::single-choice{#root-list-sudo-rules} Какая команда перечисляет привилегии sudo, доступные текущему вызывающему пользователю?

::option[`sudo -i`]{#root-sudo-login explanation="Эта команда запрашивает целевую оболочку входа и может расширить область привилегий; это не чтение списка политики."}
::option[`sudo -l`]{#root-sudo-list .correct explanation="Параметр `-l` в нижнем регистре просит sudo перечислить команды, разрешённые текущей политикой."}
::option[`su -l`]{#root-su-login-default explanation="Эта команда включает поведение оболочки входа для `su`, а не перечисляет авторизацию sudo."}
:::

## Безопасное редактирование политики sudoers

Стандартная политика sudo обычно читает `/etc/sudoers` и может включать файлы из `/etc/sudoers.d/`. Возможны и другие источники политики. Синтаксис управляет гораздо большим, чем простой список пользователей и групп.

Для изменения политики используйте `visudo`: команда блокирует файл и проверяет синтаксис перед установкой:

```bash
$ sudo visudo
```

Для дополнительного файла укажите точный путь:

```bash
$ sudo visudo -f /etc/sudoers.d/application-admins
```

Не редактируйте sudoers обычным перенаправлением или непроверенным процессом редактора. Ошибка синтаксиса или прав может лишить административного доступа. При удалённом изменении авторизации сохраняйте другой проверенный путь восстановления.

:::single-choice{#root-edit-sudoers-safely} Какой инструмент следует использовать для редактирования и проверки синтаксиса основной политики sudoers?

::option[`cat`]{#root-cat-sudoers explanation="`cat` может показать доступный текст, но не обеспечивает безопасное редактирование, блокировку или проверку синтаксиса sudoers."}
::option[`visudo`]{#root-visudo .correct explanation="`visudo` предоставляет блокировку и проверку синтаксиса, предназначенные для изменения политики sudoers."}
::option[`echo` с `>`]{#root-echo-sudoers explanation="Перенаправление оболочки может немедленно усечь политику и не проверяет синтаксис sudoers."}
:::

Чтобы попрактиковаться в делегировании администрирования в контролируемой среде, выполните лабораторную работу:

1. **[Настройка учётных записей и привилегий sudo в Linux](https://labex.io/ru/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** — отработайте политики паролей, блокировку и разблокировку учётных записей, защиту root и предоставление административных разрешений.

## Итоги

Теперь вы можете отличать переключение идентичности от управляемого политикой делегирования команд.

1. Используйте `su - USER` только когда нужна целевая оболочка входа.
2. Запрашивайте конкретную цель sudo параметром `-u USER`.
3. Сокращайте время работы в привилегированной оболочке.
4. Проверяйте действующие правила через `sudo -l`.
5. Редактируйте политику sudoers только через `visudo`.
