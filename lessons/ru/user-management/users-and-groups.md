---
lesson_id: "users-and-groups"
course_id: "user-management"
lang: "ru"
order_index: 1
title: "Пользователи и группы"
description: "Узнайте, как Linux идентифицирует пользователей и группы и как учётные данные процесса влияют на решения о доступе."
meta_title: "Пользователи и группы — управление пользователями"
meta_description: "Изучите пользователей и группы Linux, суперпользователя root, UID и GID, а также делегирование повышенных привилегий через sudo."
meta_keywords: "пользователи и группы Linux, основы Linux, sudo, пользователь root, UID, GID, управление пользователями"
---

Linux использует идентификаторы пользователей и групп, чтобы помечать процессы, назначать владельцев объектам файловой системы и принимать решения об управлении доступом. Понятные человеку имена помогают администраторам, а ядро в основном работает с числовыми идентификаторами и учётными данными процессов.

## Идентификация пользователей по UID

У каждой учётной записи есть числовой идентификатор пользователя, или **UID**. Имена пользователей сопоставляются с UID через системные базы учётных записей. Файлы хранят числового владельца, который инструменты обычно показывают как соответствующее имя.

Выполните `id`, чтобы проверить сведения об идентичности текущего процесса:

```bash
$ id
uid=1000(alice) gid=1000(alice) groups=1000(alice),27(sudo)
```

Значения различаются в разных системах. Учётные записи людей часто имеют домашние каталоги вроде `/home/alice`, но могут использовать другой путь или вовсе не иметь обычного домашнего каталога. Служебные учётные записи нередко существуют для запуска программ с ограниченной идентичностью, а не для интерактивного входа.

:::single-choice{#users-uid-purpose} Какой идентификатор ядро в основном использует для представления пользователя?

::option[Путь к домашнему каталогу]{#users-home-path explanation="Домашний путь является настраиваемым параметром учётной записи, может различаться или отсутствовать и не служит идентификатором пользователя в ядре."}
::option[Числовой UID]{#users-numeric-uid .correct explanation="Базы учётных записей сопоставляют имена числовым UID, используемым в учётных данных процессов и записях владения."}
::option[Номер окна терминала]{#users-terminal-number explanation="Устройства и сеансы терминала не связаны с числовыми идентификаторами пользователей."}
:::

## Организация доступа с помощью групп

У группы есть числовой идентификатор, или **GID**. Учётная запись обычно имеет одну первичную группу и может входить в дополнительные. Членство позволяет администраторам предоставлять доступ набору пользователей, не назначая права каждой учётной записи по отдельности.

Проверьте членство командами:

```bash
$ id alice
$ groups alice
```

Они сообщают настроенные или разрешённые сведения об идентичности. В этом могут участвовать службы каталогов и кэши, поэтому непосредственное чтение `/etc/group` не всегда показывает полную картину эффективного членства.

:::single-choice{#users-primary-supplementary-groups} Каким образом одна учётная запись Linux обычно участвует в группах?

::option[Она может состоять ровно в одной группе всю свою жизнь.]{#users-single-group explanation="Процессы Linux могут иметь первичную группу и список дополнительных."}
::option[Она входит в каждую группу, файлы которой может читать.]{#users-readable-groups explanation="Возможность чтения определяется правами и учётными данными и не создаёт членство автоматически."}
::option[У неё есть одна первичная группа и могут быть дополнительные.]{#users-group-memberships .correct explanation="Первичный GID входит в запись учётной записи, а дополнительное членство предоставляет другие групповые идентификаторы."}
:::

## Учётные данные процесса

У процесса есть такие учётные данные, как реальные и эффективные UID и GID, а также дополнительные группы. Эффективные данные играют центральную роль во многих проверках прав. Запущенный пользователем процесс обычно наследует их от родителя, но контролируемые механизмы могут изменять значения.

Это точнее утверждения, что процесс всегда работает только «от имени запустившего его пользователя». Исполняемые файлы set-user-ID, менеджеры служб, контейнеры, пространства имён и системные вызовы изменения привилегий могут влиять на видимые или эффективные идентификаторы в конкретном контексте.

:::single-choice{#users-process-access-identity} Какие данные обычно учитывает ядро при сопоставлении процесса с правами файла?

::option[Эффективные UID и GID процесса и его дополнительные группы.]{#users-effective-credentials .correct explanation="При обычной дискреционной проверке доступа эти данные сравниваются с владельцами и правами."}
::option[Цветовая тема терминала, запустившего процесс.]{#users-terminal-theme explanation="Настройки отображения не участвуют в проверке прав файловой системы."}
::option[Длина имени учётной записи.]{#users-username-length explanation="Ядро работает с числовыми учётными данными; длина имени не предоставляет доступ."}
:::

## Идентичность root

Учётная запись с традиционным именем `root` имеет UID 0. Многие механизмы прав Linux обрабатывают UID 0 особо, предоставляя широкие административные возможности. Современный Linux также разделяет привилегии через capabilities, пространства имён, обязательное управление доступом и изоляцию служб, поэтому «неограниченная власть в любом контексте» — чрезмерное упрощение.

Повседневную работу следует выполнять из непривилегированной учётной записи. Административные полномочия усиливают последствия ошибок в путях, ненадёжных команд и скомпрометированного ПО.

:::single-choice{#users-root-uid} Какой числовой UID традиционно обозначает учётную запись root?

::option[`0`]{#users-uid-zero .correct explanation="Linux и Unix-подобные системы традиционно резервируют UID 0 для идентичности суперпользователя."}
::option[`1000`]{#users-uid-thousand explanation="Многие дистрибутивы назначают значение около 1000 первой обычной учётной записи человека, но это не UID root."}
::option[`1`]{#users-uid-one explanation="UID 1 может принадлежать системной учётной записи и не является традиционной идентичностью суперпользователя."}
:::

## Использование sudo согласно политике

`sudo` запрашивает настроенную политику, разрешено ли вызывающему пользователю выполнить команду от имени целевого. По умолчанию целью часто является root, но политика или `-u USER` может выбрать другую учётную запись. Запросы аутентификации и журналирование также зависят от конфигурации.

Выведите команды, которые разрешено выполнять текущей учётной записи:

```bash
$ sudo -l
```

Используйте разрешённую административную команду только тогда, когда она нужна для задачи и вы понимаете её последствия. Не применяйте `sudo` лишь для подавления ошибки прав и не показывайте базы хешей паролей вроде `/etc/shadow` как случайное упражнение.

:::single-choice{#users-sudo-policy} Что делает `sudo` перед запуском запрошенной команды?

::option[Проверяет настроенную политику на разрешение использовать целевую идентичность.]{#users-sudo-policy-check .correct explanation="`sudo` выполняет авторизацию согласно политике и при разрешении устанавливает настроенные целевые учётные данные."}
::option[Всегда предоставляет каждому локальному пользователю неограниченный доступ root.]{#users-sudo-always-root explanation="Авторизация управляется политикой; пользователи и команды без разрешения не получают полный доступ root."}
::option[Навсегда изменяет UID вызывающей учётной записи на 0.]{#users-sudo-permanent-uid explanation="`sudo` запускает команду с целевыми учётными данными и не переписывает идентичность вызывающего навсегда."}
:::

Чтобы попрактиковаться в администрировании учётных записей и групп в контролируемой среде, выполните лабораторные работы:

1. **[Управление учётными записями Linux с useradd, usermod и userdel](https://labex.io/ru/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** — отработайте полный жизненный цикл администрирования пользователей: создание, защита, изменение и удаление учётных записей.
2. **[Управление группами Linux с groupadd, usermod и groupdel](https://labex.io/ru/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** — попрактикуйтесь создавать группы, менять членство пользователей и удалять группы.
3. **[Настройка учётных записей и привилегий sudo в Linux](https://labex.io/ru/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** — освойте управление пользователями и привилегиями `sudo`, включая предоставление административных разрешений.

## Итоги

Теперь вы можете описать представление идентичностей и делегирование административных команд в Linux.

1. Идентифицируйте учётные записи по UID, а группы — по GID.
2. Отличайте первичную группу от дополнительных.
3. Связывайте учётные данные процесса с проверками доступа.
4. Распознавайте UID 0 как традиционную идентичность root.
5. Считайте `sudo` инструментом делегирования под управлением политики.
