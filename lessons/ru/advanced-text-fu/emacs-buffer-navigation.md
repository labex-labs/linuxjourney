---
lesson_id: "emacs-buffer-navigation"
course_id: "advanced-text-fu"
lang: "ru"
order_index: 11
title: "Навигация по буферам Emacs"
description: "Узнайте, как переключать и уничтожать буферы Emacs, а также разделять, выбирать и закрывать окна отображения."
meta_title: "Навигация по буферам Emacs - Продвинутый Text-Fu"
meta_description: "Полное руководство по навигации по буферам Emacs. Научитесь эффективно переключать буферы, разделять окна и управлять рабочим процессом с помощью основных команд Emacs. Освойте команду переключения буферов Emacs и улучшите навыки редактирования текста."
meta_keywords: "навигация emacs, переключение буферов emacs, управление буферами emacs, команды emacs, C-x b, C-x k, C-x 2, текстовый редактор, linux"
---

Буфер Emacs хранит текст или состояние редактора, а окно показывает буфер. Буфер может существовать невидимым, и несколько окон способны отображать один буфер. Управление одним объектом не управляет другим автоматически.

## Переключение буферов

Нажмите `C-x b`, что запускает `switch-to-buffer`, чтобы выбрать по имени буфер для текущего окна:

```text
C-x b
```

Минибуфер предлагает дополнение существующих имён. Ввод нового имени может создать нефайловый буфер с таким именем; путь к файлу при этом не открывается.

По умолчанию `C-x Right` запускает `next-buffer`, а `C-x Left` — `previous-buffer`, циклически перебирая буферы в выбранном окне.

:::single-choice{#emacs-switch-buffer-key} Какая последовательность запрашивает имя буфера для показа в текущем окне?

::option[`C-x C-f`]{#emacs-buffer-find-file explanation="Она запрашивает путь и открывает файл, что отличается от выбора существующего буфера по имени."}
::option[`C-x b`]{#emacs-switch-buffer .correct explanation="`switch-to-buffer` читает имя буфера и показывает его в выбранном окне."}
::option[`C-x k`]{#emacs-buffer-kill explanation="Она предлагает уничтожить буфер, а не переключить на него выбранное окно."}
:::

## Разделение выбранного окна

Нажмите `C-x 2`, чтобы разделить выбранное окно на верхнее и нижнее:

```text
C-x 2
```

Нажмите `C-x 3`, чтобы получить левое и правое окна:

```text
C-x 3
```

Новое окно сначала показывает какой-либо буфер, часто тот же самый. В каждом окне можно независимо переключить буфер.

:::single-choice{#emacs-split-side-by-side} Какая последовательность разделяет выбранное окно Emacs на левое и правое?

::option[`C-x 1`]{#emacs-window-one explanation="Она удаляет остальные окна и оставляет выбранное единственным во фрейме."}
::option[`C-x 2`]{#emacs-window-below explanation="Она создаёт верхнее и нижнее окна, а не расположенные рядом."}
::option[`C-x 3`]{#emacs-window-right .correct explanation="`split-window-right`, привязанная к `C-x 3`, создаёт левое и правое окна."}
:::

## Выбор и закрытие окон

Нажмите `C-x o`, что запускает `other-window`, чтобы выбрать следующее окно:

```text
C-x o
```

Для удаления отображаемых окон используйте:

- `C-x 0`: удалить выбранное окно.
- `C-x 1`: удалить остальные окна текущего фрейма.

Удаление окна обычно оставляет показанный в нём буфер существовать. Позже его можно снова вывести в другом окне.

:::single-choice{#emacs-select-other-window} Какая последовательность переносит point и клавиатурный фокус в другое окно Emacs?

::option[`C-x 0`]{#emacs-delete-selected-window explanation="Она удаляет выбранное окно, а не переводит фокус в другое."}
::option[`C-x o`]{#emacs-other-window .correct explanation="`other-window` циклически выбирает другое окно во фрейме."}
::option[`C-x b`]{#emacs-switch-in-window explanation="Она меняет буфер, который показывает текущее окно, а не выбранное окно."}
:::

:::single-choice{#emacs-keep-one-window} Какая последовательность сохраняет выбранное окно и удаляет остальные окна его фрейма?

::option[`C-x 1`]{#emacs-delete-other-windows .correct explanation="`delete-other-windows` делает выбранное окно единственным во фрейме."}
::option[`C-x 0`]{#emacs-delete-current-window explanation="Она удаляет само выбранное окно вместо его сохранения."}
::option[`C-x 2`]{#emacs-add-lower-window explanation="Она добавляет ещё одно окно, а не оставляет во фрейме одно."}
:::

## Уничтожение буфера

Нажмите `C-x k`, что запускает `kill-buffer`, чтобы выбрать удаляемый из Emacs буфер:

```text
C-x k
```

По умолчанию предлагается текущий буфер. Если в файловом буфере есть несохранённые изменения, Emacs предупреждает перед уничтожением. Читайте запрос: уничтожение изменённого буфера может отбросить правки.

Уничтожение буфера отличается от удаления окна. Во всех показывающих уничтоженный буфер окнах Emacs заменяет его другим, тогда как удаление окна может не затронуть буфер.

:::single-choice{#emacs-kill-buffer-key} Какая последовательность предлагает уничтожить буфер Emacs?

::option[`C-x 0`]{#emacs-kill-window-only explanation="Она удаляет отображающее окно, но обычно оставляет буфер существовать."}
::option[`C-x k`]{#emacs-kill-buffer-answer .correct explanation="`kill-buffer` удаляет выбранный буфер из Emacs после необходимого подтверждения для изменённого буфера."}
::option[`C-x b`]{#emacs-kill-switch explanation="Она переключает текущее окно на именованный буфер и не уничтожает его."}
:::

Тренируйтесь с `*scratch*` и одноразовыми буферами. Перед уничтожением файлового буфера проверьте индикатор изменений и наличие несохранённой работы.

## Итоги

Теперь вы умеете управлять тем, что хранит Emacs и что показывает каждое окно.

1. Переключать буферы выбранного окна через `C-x b`.
2. Разделять окно вниз через `C-x 2` или вправо через `C-x 3`.
3. Выбирать другое окно через `C-x o`.
4. Удалять отображаемые окна через `C-x 0` или `C-x 1`.
5. Уничтожать буфер через `C-x k` только после проверки несохранённых правок.
