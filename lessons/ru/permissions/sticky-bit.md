---
lesson_id: "sticky-bit"
course_id: "permissions"
lang: "ru"
order_index: 8
title: "Sticky-бит"
description: "Узнайте, как sticky-бит защищает записи в общих каталогах с правом записи, например /tmp."
meta_title: "Sticky-бит — права доступа"
meta_description: "Изучите назначение sticky-бита в правах Linux и Unix, защиту файлов в общих каталогах вроде /tmp и его установку через chmod."
meta_keywords: "sticky-бит, sticky-бит Linux, sticky-бит прав Unix, chmod +t, каталог /tmp, права файлов, безопасность Linux"
---

Каталог с правом записи обычно позволяет авторизованному пользователю удалять или переименовывать записи в нём, даже если сами файлы принадлежат другому пользователю. Sticky-бит добавляет ограничение по владению, делая общие записываемые каталоги безопаснее.

## Как sticky-бит ограничивает удаление

Если у каталога установлен sticky-бит, Linux обычно разрешает удалить или переименовать запись только процессу с подходящими привилегиями, владельцу каталога или владельцу записи. Обычные права записи и поиска каталога всё равно необходимы.

Ограничение относится к записям каталога. Оно не мешает владельцу файла изменять его содержимое, если права файла разрешают операцию, и не делает каталог закрытым.

:::single-choice{#sticky-bit-removal-rule}
Какой обычный пользователь обычно может удалить конкретную запись в общем каталоге со sticky-битом?

::option[Любой пользователь, способный перечислить каталог.]{#sticky-bit-any-reader explanation="Право чтения каталога может раскрыть имена, но не отменяет ограничение sticky по владению."}
::option[Владелец записи при наличии требуемого доступа к каталогу.]{#sticky-bit-entry-owner .correct explanation="Владелец записи — один из идентификаторов, которым правило sticky-каталога обычно разрешает удаление."}
::option[Только член группы записи.]{#sticky-bit-entry-group explanation="Одно членство в группе не является исключением по владению, определяемым sticky-битом."}
:::

## Распознавание бита у `/tmp`

Распространённым примером служит системный каталог временных файлов:

```bash
$ ls -ld /tmp
drwxrwxrwt 17 root root 4096 Dec 15 11:45 /tmp
```

Конечная строчная `t` занимает позицию выполнения остальных. Она означает наличие и sticky-бита, и права выполнения остальных. Прописная `T` означает, что sticky-бит установлен, а право выполнения остальных отсутствует.

Поскольку `/tmp` обычно доступен всем для записи и поиска, несколько пользователей могут создавать в нём записи. Sticky-бит не позволяет обычному пользователю удалять записи другого лишь потому, что каталог доступен для записи всем. Приложения всё равно должны безопасно создавать временные объекты: предсказуемые имена, небезопасные ссылки и слабые режимы файлов несут отдельные риски.

:::single-choice{#sticky-bit-lowercase-t}
Что означает строчная `t` в конце режима каталога?

::option[Установлены sticky-бит и выполнение остальных.]{#sticky-bit-t-with-execute .correct explanation="Строчная t сочетает специальный sticky-бит с обычным битом выполнения остальных."}
::option[Sticky-бит установлен, а выполнение остальных отсутствует.]{#sticky-bit-t-without-execute explanation="Такое сочетание отображается прописной T."}
::option[Установлены setgid и выполнение группы.]{#sticky-bit-setgid-position explanation="Setgid показывается в позиции выполнения группы, а не в конечной позиции остальных."}
:::

## Установка и удаление sticky-бита

Установите бит символически:

```bash
$ chmod +t shared-directory
```

В начальной восьмеричной цифре специальных битов sticky добавляет `1`:

```bash
$ chmod 1777 shared-directory
```

Начальная `1` устанавливает sticky, а `777` задаёт обычный режим. Такой режим уместен только для каталога, намеренно общего для всех локальных пользователей. Для командного каталога могут быть предпочтительнее более узкие групповые права. Удалите только sticky-бит командой `chmod -t shared-directory`.

:::single-choice{#sticky-bit-octal-value}
Какое начальное восьмеричное значение обозначает sticky-бит?

::option[`2`]{#sticky-bit-value-two explanation="Начальная 2 обозначает setgid."}
::option[`1`]{#sticky-bit-value-one .correct explanation="Sticky-бит добавляет 1 к начальной цифре специальных битов."}
::option[`4`]{#sticky-bit-value-four explanation="Начальная 4 обозначает setuid."}
:::

## Проверка полной политики каталога

Sticky не предоставляет доступ на запись или поиск: он лишь ограничивает удаление и переименование после того, как обычные права разрешили изменение каталога. Проверяйте вместе владельца, группу, обычный режим, ACL и контекст подключения каталога. Тестируйте с непривилегированными учётными записями в изолированной среде, а не изменяйте `/tmp` рабочей системы.

:::single-choice{#sticky-bit-access-scope}
Делает ли добавление sticky-бита незаписываемый каталог доступным для записи другим пользователям?

::option[Да; sticky автоматически добавляет запись каждому классу.]{#sticky-bit-adds-write explanation="Специальный бит не переписывает биты записи владельца, группы или остальных."}
::option[Да; sticky отключает тройку прав остальных у каталога.]{#sticky-bit-disables-other explanation="Тройка остальных продолжает участвовать в обычных проверках доступа."}
::option[Нет; доступ по-прежнему определяется обычными правами записи и поиска.]{#sticky-bit-no-write-grant .correct explanation="Sticky сужает некоторые операции удаления и переименования, но не добавляет отсутствующие обычные права."}
:::

Для практики создайте одноразовый общий каталог, задайте подходящий обычный режим и sticky-бит, затем проверьте удаление записей от имени двух непривилегированных пользователей. Лаборатория [Удаление и перемещение файлов](https://labex.io/ru/labs/linux-delete-and-move-files-7777) поможет закрепить основные операции переименования и удаления.

## Итоги

Теперь вы можете объяснять и проверять sticky-бит у общих каталогов.

1. Связывайте sticky с ограничениями по владению при удалении и переименовании.
2. Распознавайте строчную `t` и прописную `T` в подробном списке.
3. Устанавливайте бит символически или начальным восьмеричным значением `1`.
4. Оценивайте sticky вместе с обычными правами каталога.
