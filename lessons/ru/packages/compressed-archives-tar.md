---
lesson_id: "compressed-archives-tar"
course_id: "packages"
lang: "ru"
order_index: 3
title: "tar и gzip"
description: "Узнайте, как архивировать файлы с помощью tar, сжимать потоки через gzip и проверять архивы перед безопасным извлечением."
meta_title: "tar и gzip — пакеты"
meta_description: "Подробное руководство по tar и gzip в Linux: создание и извлечение архивов, сжатие tar.gz и различие между tar и gzip."
meta_keywords: "tar и gzip, сжатие tar, gzip tar, сжать tar gz, gzip и tar, архивация Linux, сжатие файлов, команда tar, команда gzip, учебник Linux"
---

Архивация и сжатие решают разные задачи. Архив объединяет дерево каталогов и его метаданные в один поток. Сжатие кодирует поток, уменьшая его размер. Файл `.tar.gz` по соглашению является архивом tar, поток которого сжат с помощью gzip.

## Сжатие одного потока с помощью `gzip`

По умолчанию `gzip` сжимает файл и заменяет исходное имя файлом с расширением `.gz`:

```bash
$ gzip report.txt
```

Обычно после успешного создания `report.txt.gz` файл `report.txt` удаляется. Распакуйте его командой:

```bash
$ gunzip report.txt.gz
```

Там, где это поддерживается, используйте `gzip -k report.txt`, чтобы сохранить входной файл, либо применяйте стандартные потоки для явного управления. Расширение имени — соглашение, а не доказательство настоящего формата; содержимое можно проверить инструментами вроде `file`.

:::single-choice{#tar-gzip-gzip-role}
Каково основное назначение `gzip` в этом уроке?

::option[Объединение дерева каталогов в архив с метаданными файлов.]{#tar-gzip-directory-archive explanation="Эту функцию архивации выполняет tar до применения сжатия gzip."}
::option[Сжатие одного входного потока.]{#tar-gzip-compress-stream .correct explanation="Gzip преобразует один поток байтов и сам не кодирует иерархию каталогов."}
::option[Установка метаданных зависимостей в базу пакетов.]{#tar-gzip-package-install explanation="Сжатие не связано с установкой собственных пакетов и отслеживанием зависимостей."}
:::

## Создание архива tar

Создайте несжатый архив:

```bash
$ tar -cvf project.tar file1 file2 directory1
```

- `-c` создаёт новый архив.
- `-v` перечисляет элементы во время обработки и является необязательным.
- `-f project.tar` задаёт имя файла архива; поскольку `-f` принимает аргумент, располагайте имя рядом с ним.

Пути хранятся как имена элементов архива. Создавайте архивы из намеренно выбранного рабочего каталога и не захватывайте случайно секреты, кэши, сокеты или широкие абсолютные пути.

:::single-choice{#tar-gzip-create-option}
Какой параметр `tar` создаёт новый архив?

::option[`-x`]{#tar-gzip-option-extract explanation="Операция -x извлекает элементы архива."}
::option[`-c`]{#tar-gzip-option-create .correct explanation="Операция создания записывает новый архив из указанных входных данных."}
::option[`-t`]{#tar-gzip-option-list explanation="Операция -t перечисляет элементы архива без извлечения."}
:::

## Создание сжатого gzip архива tar

GNU tar и многие другие реализации могут вызвать gzip параметром `-z`:

```bash
$ tar -czvf project.tar.gz file1 file2 directory1
```

Результатом будет один сжатый gzip поток tar. Сжатие не шифрует архив и не скрывает его содержимое от того, кто может прочитать и распаковать файл. Если нужна конфиденциальность, используйте подходящий процесс аутентифицированного шифрования и управляйте ключами отдельно.

:::single-choice{#tar-gzip-z-option}
Что запрашивает `-z` в показанной команде `tar`?

::option[Зашифровать архив ключом с нулевым разглашением.]{#tar-gzip-z-encrypt explanation="Ни tar, ни gzip не обеспечивают шифрование этим параметром."}
::option[Отбросить все элементы нулевого размера.]{#tar-gzip-z-zero explanation="Параметр выбирает gzip и не фильтрует элементы архива по размеру."}
::option[Обработать поток архива через gzip.]{#tar-gzip-z-gzip .correct explanation="Параметр `z` соединяет операцию архивации tar со сжатием или распаковкой gzip."}
:::

## Перечисление перед извлечением

Считайте архив от другой стороны недоверенными входными данными. Сначала перечислите имена его элементов:

```bash
$ tar -tzf download.tar.gz
```

Ищите неожиданные абсолютные пути, компоненты обхода `..`, подозрительные символические или жёсткие ссылки, файлы устройств и имена, которые перезапишут важные файлы. Современные реализации tar применяют защитные меры, однако поведение и параметры различаются, а при извлечении всё равно создаются выбранные атакующим имена и содержимое.

Извлекайте в новый непривилегированный промежуточный каталог:

```bash
$ mkdir extraction-stage
$ tar -xzf download.tar.gz -C extraction-stage
```

Не извлекайте непроверенный архив от имени root. Проверьте созданные объекты до переноса выбранных файлов в конечные места.

:::single-choice{#tar-gzip-list-before-extract}
Какая операция перечисляет элементы архива без их извлечения?

::option[`tar -czf download.tar.gz .`]{#tar-gzip-create-download explanation="Команда создаёт или заменяет архив из текущего каталога."}
::option[`tar -xzf download.tar.gz`]{#tar-gzip-extract-download explanation="Операция -x записывает элементы в целевой каталог."}
::option[`tar -tzf download.tar.gz`]{#tar-gzip-list-members .correct explanation="Операция -t читает и отображает таблицу элементов, а -z обрабатывает gzip."}
:::

## Другие форматы сжатия

Реализации tar могут работать со средствами сжатия bzip2 и xz, которые в GNU tar обычно выбираются соответственно через `-j` и `-J`. Поддержка форматов и автоматическое обнаружение различаются, поэтому обращайтесь к `tar --help` или локальному руководству. ZIP — отдельный формат архива, с которым работают инструменты `zip` и `unzip`.

:::single-choice{#tar-gzip-archive-confidentiality}
Делает ли сжатие gzip архив tar конфиденциальным?

::option[Нет; любой, кто может прочитать архив, обычно может его распаковать.]{#tar-gzip-not-encryption .correct explanation="Сжатие меняет представление и размер, но не обеспечивает управление доступом или криптографическую секретность."}
::option[Да; gzip получает ключ шифрования из имени файла.]{#tar-gzip-filename-key explanation="Gzip не реализует такой механизм шифрования."}
::option[Да; tar шифрует каждый элемент до передачи gzip.]{#tar-gzip-tar-encrypt explanation="Tar архивирует элементы, но не шифрует их содержимое автоматически."}
:::

Потренируйтесь на одноразовых файлах в лаборатории [Упаковка и сжатие файлов](https://labex.io/ru/labs/linux-file-packaging-and-compression-385413), затем примените проверку и промежуточный каталог в лаборатории [Создание и восстановление резервной копии с помощью tar](https://labex.io/ru/labs/comptia-create-and-restore-a-backup-with-tar-in-linux-590843).

## Итоги

Теперь вы можете безопасно сочетать архивацию tar со сжатием gzip.

1. Отличайте архив tar от сжатия gzip.
2. Создавайте архивы через `-c`, а потоки gzip — через `-z`.
3. Перечисляйте элементы через `-t` до извлечения через `-x`.
4. Извлекайте недоверенное содержимое в непривилегированный промежуточный каталог.
5. Рассматривайте сжатие отдельно от шифрования.
