---
lesson_id: "arp-command"
course_id: "network-config"
lang: "ru"
order_index: 5
title: "arp"
description: "Узнайте, как проверять и интерпретировать состояние кэша ARP для IPv4 и соседей IPv6 в Linux."
meta_title: "arp — настройка сети"
meta_description: "Познакомьтесь с командой ARP в Linux, просмотром кэша ARP и ролью протокола в сетевом взаимодействии."
meta_keywords: "ARP Linux, кэш ARP, ip neighbour show, сетевые команды, сети Linux, Linux для начинающих, учебник Linux"
---

Linux хранит недавно разрешённые канальные адреса следующих переходов в таблице соседей. Для IPv4 поверх Ethernet записи изучаются через ARP, а IPv6 использует обнаружение соседей. Устаревшая команда `arp` показывает лишь часть этого состояния, тогда как `ip neighbor` работает с обоими семействами.

## Просмотр записей соседей

Проверьте все записи или записи одного интерфейса:

```bash
$ ip neighbor show
$ ip neighbor show dev enp1s0
```

Запись содержит IP-адрес, адрес канального уровня, устройство и состояние доступности. После загрузки таблица может быть пустой и заполняться по мере того, как трафику требуются локальные следующие переходы.

:::single-choice{#arp-command-modern-view} Какая команда отображает современное состояние таблицы соседей Linux?

::option[`pwd neighbor`]{#arp-command-pwd explanation="Pwd выводит рабочий каталог оболочки."}
::option[`ip neighbor show`]{#arp-command-ip-neighbor .correct explanation="Она показывает как полученные через ARP записи IPv4, так и записи обнаружения соседей IPv6."}
::option[`route --passwords`]{#arp-command-route-passwords explanation="Ни одна проверка маршрутов не должна раскрывать учётные данные."}
:::

## Разрешение соседа IPv4

Если сопоставление для локального IPv4-адреса отсутствует, хост отправляет широковещательный запрос ARP с вопросом о владельце целевого адреса. Отвечает сам целевой узел либо маршрутизатор, явно выполняющий proxy ARP. Отправитель кэширует сопоставление и передаёт ожидающий кадр.

Для удалённого IP-адреса назначения хост разрешает адрес выбранного шлюза, а не MAC-адрес удалённого узла.

:::single-choice{#arp-command-remote-target} Какого соседа IPv4 разрешает хост для назначения вне локального канала?

::option[Конечный удалённый сервер за всеми маршрутизаторами.]{#arp-command-final-server explanation="Его MAC-адрес не имеет смысла в исходном канале."}
::option[Каждый DNS-сервер из конфигурации разрешения имён.]{#arp-command-all-dns explanation="Разрешение соседа следует выбранному маршруту, а не списку DNS."}
::option[Выбранный локальный шлюз.]{#arp-command-gateway .correct explanation="Локальный кадр Ethernet адресуется маршрутизатору, который пересылает IP-пакет."}
:::

## Интерпретация состояний

К распространённым состояниям относятся `REACHABLE`, `STALE`, `DELAY`, `PROBE`, `INCOMPLETE` и `FAILED`. `STALE` означает, что срок недавнего подтверждения доступности истёк; кэшированный адрес всё ещё можно использовать, пока сетевой стек при необходимости выполняет проверку. `FAILED` указывает на неудачное разрешение или определение доступности, однако причиной может быть канал, VLAN, адрес, маршрут, фильтрация либо отключённый удалённый узел.

:::single-choice{#arp-command-stale-state} Означает ли `STALE`, что сосед точно недоступен?

::option[Нет; недавнего подтверждения нет, и при использовании можно выполнить проверку.]{#arp-command-stale-probe .correct explanation="Это состояние не равнозначно FAILED."}
::option[Да, и запись больше никогда нельзя использовать.]{#arp-command-stale-dead explanation="Устаревшие записи остаются кандидатами и могут сменить состояние после проверки доступности."}
::option[Да, поскольку срок действия записи DNS истёк.]{#arp-command-stale-dns explanation="Состояние соседа и кэширование DNS не связаны."}
:::

## Осторожное изменение состояния соседей

Статические записи и очистка кэша изменяют состояние, могут нарушить активный трафик и скрыть исходные свидетельства. Сначала сохраните текущие маршруты, счётчики пакетов и состояние соседей. До очистки всего интерфейса предпочтите целевую проверку и захват пакетов в разрешённой тестовой сети.

В ARP нет встроенной аутентификации, поэтому дублирующиеся адреса или поддельные ответы способны отравить сопоставления. Защита на коммутаторах, сегментация, мониторинг и аутентификация верхних уровней помогают снизить последствия.

:::single-choice{#arp-command-flush-first} Почему не следует начинать диагностику с очистки всей таблицы соседей?

::option[Записи соседей хранятся только на корневых серверах DNS.]{#arp-command-neighbors-dns explanation="Их обслуживает локальный сетевой стек."}
::option[Очистка навсегда удаляет оборудование интерфейса.]{#arp-command-flush-hardware explanation="Она удаляет записи кэша, а не физические устройства."}
::option[Она изменяет свидетельства и может прервать работу исправных следующих переходов.]{#arp-command-flush-disrupts .correct explanation="Проверка только для чтения и целевые тесты сохраняют состояние, необходимое для диагностики причины."}
:::

## Итоги

Теперь вы можете проверять разрешение соседей, не считая каждое состояние кэша признаком сбоя.

1. Используйте `ip neighbor` для состояния IPv4 и IPv6.
2. Разрешайте сам адрес назначения, только если он находится в локальном канале.
3. Для IP-трафика вне локального канала разрешайте шлюз.
4. Сохраняйте свидетельства кэша до целевых изменений состояния.
