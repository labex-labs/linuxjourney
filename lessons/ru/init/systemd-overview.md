---
lesson_id: "systemd-overview"
course_id: "init"
lang: "ru"
order_index: 5
title: "Обзор systemd"
description: "Узнайте, как systemd загружает units, разрешает зависимости, активирует targets и управляет системными и пользовательскими ресурсами."
meta_title: "Обзор systemd - Init"
meta_description: "Изучите основы systemd: units и targets, управление загрузкой Linux и системными службами и основные понятия современной инициализации."
meta_keywords: "systemd, система инициализации, units systemd, targets systemd, загрузка linux, службы linux, управление системой"
---

Systemd — система инициализации PID 1 и менеджер служб, используемый многими современными дистрибутивами. Проект systemd также предоставляет компоненты журналирования, устройств, входа, сети, времени и другие, но дистрибутивы сами выбирают внедряемые части.

## Подтверждение работающего менеджера

Проверяйте живое состояние, а не наличие установленных каталогов:

```bash
$ ps -p 1 -o pid,comm,args=
$ systemctl is-system-running
```

`/usr/lib/systemd/` может существовать при другом PID 1, а контейнер способен предоставлять собственное пространство PID. У `systemctl` также есть режимы пользовательского менеджера, удалённой системы и контейнера, поэтому определяйте цель операции.

:::single-choice{#systemd-overview-detection}
Что наиболее прямо определяет systemd как системный менеджер init?

::option[Существует каталог `/usr/lib/systemd`.]{#systemd-overview-directory explanation="Библиотеки и unit-файлы могут оставаться установленными, когда systemd не действует как PID 1."}
::option[Пользователь выполнил одну команду `systemctl`.]{#systemd-overview-command-executed explanation="Клиентская программа может существовать без доступного системного менеджера systemd."}
::option[PID 1 хоста является systemd.]{#systemd-overview-pid-one .correct explanation="Работающий первый процесс — более сильное доказательство, чем установленные файлы или имена пакетов."}
:::

## Units как управляемые объекты

Unit — именованная модель ресурса или действия systemd. Распространённые типы:

- `.service` для процессов и демонов;
- `.socket` для активации через сокет;
- `.mount` и `.automount` для файловых систем;
- `.timer` и `.path` для событийной активации;
- `.target` для группировки и синхронизации;
- `.device`, `.swap`, `.slice` и `.scope` для других ресурсов.

Состояние unit не всегда «running»: mount может быть смонтирован, timer — ждать, device — присутствовать, target — оставаться активным после достижения зависимостей.

:::single-choice{#systemd-overview-group-unit}
Какой тип unit обычно группирует другие units и предоставляет точку синхронизации?

::option[`.socket`]{#systemd-overview-socket explanation="Socket units предоставляют IPC- или сетевые конечные точки и могут активировать службы."}
::option[`.target`]{#systemd-overview-target .correct explanation="Target units собирают зависимости и представляют этапы загрузки или работы."}
::option[`.timer`]{#systemd-overview-timer explanation="Timer units планируют активацию по календарному или монотонному времени."}
:::

## Пути загрузки units и переопределения

Системные units могут загружаться из путей дистрибутива и администратора:

- `/usr/lib/systemd/system/` — units пакетов во многих дистрибутивах;
- `/run/systemd/system/` — сгенерированная во время выполнения или временная конфигурация;
- `/etc/systemd/system/` — постоянная локальная конфигурация и переопределения администратора.

Точный путь поставщика может различаться. Более приоритетная локальная конфигурация переопределяет файл с тем же именем ниже. Предпочитайте drop-in через `systemctl edit UNIT` копированию и изменению целого файла поставщика, чтобы обновления пакета оставались видимыми.

:::single-choice{#systemd-overview-local-override}
Где обычно должны находиться постоянные локальные переопределения системных units?

::option[Внутри `/proc/systemd/`.]{#systemd-overview-proc-systemd explanation="Procfs — runtime-интерфейс ядра, а не постоянная конфигурация units."}
::option[В `/etc/systemd/system/`.]{#systemd-overview-etc-system .correct explanation="Уровень конфигурации администратора имеет приоритет над пакетными units поставщика."}
::option[В байтах загрузочного кода MBR.]{#systemd-overview-mbr-units explanation="Units служб — конфигурационные файлы пользовательского пространства."}
:::

## Зависимости и порядок

Systemd строит транзакцию из связей зависимостей. `Wants=` и `Requires=` с разной силой добавляют другие units. `Before=` и `After=` задают порядок, если обе units запланированы, но сами по себе не запускают другую unit.

`After=network.target` не доказывает готовность соединения, DNS или конкретной удалённой точки. Службы должны применять подходящую интеграцию network-online либо собственные повторы и проверку готовности.

:::single-choice{#systemd-overview-after-semantics}
Что само по себе задаёт `After=other.service`?

::option[Гарантию здоровья конечной точки другого приложения.]{#systemd-overview-after-health explanation="Завершение упорядочивания и готовность приложения — разные понятия."}
::option[Порядок, если обе units входят в транзакцию.]{#systemd-overview-after-ordering .correct explanation="Для добавления другой unit нужна отдельная зависимость Wants или Requires."}
::option[Автоматическое включение обеих units при каждой будущей загрузке.]{#systemd-overview-after-enable explanation="Включение является установочными метаданными и не следует из порядка."}
:::

## Targets и транзакция загрузки по умолчанию

`default.target` обычно является псевдонимом target вроде `multi-user.target` или `graphical.target`. Systemd запускает транзакцию этой target и её зависимостей, позволяя независимой работе идти параллельно при соблюдении явного порядка.

Targets похожи на runlevel лишь на общем уровне совместимости. Одновременно могут быть активны несколько targets, можно создавать пользовательские, а активность target не означает здоровье каждой службы машины.

:::single-choice{#systemd-overview-default-target}
Что обычно выбирает `default.target`?

::option[Блочное устройство по умолчанию, которое должен стереть `mkfs`.]{#systemd-overview-default-disk explanation="Targets описывают активацию units, а не выбор хранилища для разрушительных операций."}
::option[Единственную target, которая может быть активна.]{#systemd-overview-only-target explanation="Targets являются группами, и в одной загрузке могут быть активны многие."}
::option[Транзакцию target для обычной загрузки системы.]{#systemd-overview-normal-boot .correct explanation="Обычно это псевдоним выбранной администратором многопользовательской или графической target."}
:::

## Итоги

Теперь вы можете описать systemd через живые менеджеры, units и транзакции.

1. Подтверждать systemd через нужный PID 1 и соединение с менеджером.
2. Сопоставлять типы ресурсов суффиксам units.
3. Размещать локальные переопределения выше конфигурации поставщика.
4. Разделять силу зависимости, порядок и готовность приложения.
5. Считать targets группировками и этапами, а не эксклюзивными состояниями.
