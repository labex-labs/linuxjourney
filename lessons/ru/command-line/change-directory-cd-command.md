---
lesson_id: "change-directory-cd-command"
course_id: "command-line"
lang: "ru"
order_index: 3
title: "cd (Смена каталога)"
description: "Научитесь использовать cd с путями и сокращениями для перемещения по файловой системе Linux."
meta_title: "cd (Смена каталога) - Командная строка"
meta_description: "Изучите команду Linux cd с примерами абсолютных и относительных путей, ярлыков домашнего каталога, перехода к родительским и предыдущим каталогам."
meta_keywords: "команда cd, linux команда cd, смена каталога, cd родительский каталог, cd домой, cd предыдущий каталог, абсолютный путь, относительный путь"
---

Для перемещения по файловой системе Linux путь указывает назначение. Главный инструмент — команда `cd`, сокращение от change directory: она меняет текущий рабочий каталог оболочки.

Назначение должно быть каталогом, а не обычным файлом. Если каталог не существует, имя введено неверно или у вас нет разрешения войти, `cd` сообщает об ошибке и не меняет положение.

Базовый синтаксис:

```bash
cd [DIRECTORY]
```

## Понимание путей

Путь бывает абсолютным или относительным.

- **Абсолютный путь** — полный путь от корневого каталога (`/`), например `/home/pete/Desktop`.
- **Относительный путь** — путь от текущего положения. Находясь в `/home/pete/Documents`, в подкаталог `taxes` можно перейти по пути `taxes/`.

:::single-choice{#recognize-absolute-cd-path} Какое утверждение правильно описывает абсолютный путь?

::option[Он начинается в каталоге, который сейчас использует оболочка.]{#begins-at-current-directory explanation="Путь, зависящий от текущего положения оболочки, является относительным и не обязательно начинается от корня."}
::option[Он содержит только имя конечного каталога без родителей.]{#contains-final-name-only explanation="Одиночное имя обычно интерпретируется относительно текущего каталога, а абсолютный путь включает маршрут от `/`."}
::option[Он начинается в корневом каталоге, обозначаемом `/`.]{#begins-at-root .correct explanation="Абсолютный путь идёт от корня файловой системы; ведущий `/` делает его независимым от текущего каталога."}
:::

## Использование команды cd

Чтобы перейти по абсолютному пути, введите:

```bash
$ cd /home/pete/Pictures
```

Команда сразу перемещает в каталог `Pictures`.

Проверьте положение через `pwd`:

```bash
$ pwd
/home/pete/Pictures
```

:::single-choice{#verify-changed-directory} Какая команда подтверждает текущее положение оболочки после `cd`?

::option[`cd`]{#cd-command explanation="`cd` меняет каталог, но обычно не выводит его полный путь; для проверки используйте `pwd`."}
::option[`ls`]{#ls-command explanation="`ls` показывает содержимое каталога, тогда как само положение сообщает `pwd`."}
::option[`pwd`]{#pwd-command .correct explanation="`pwd` выводит текущий рабочий каталог и позволяет проверить, куда `cd` переместила оболочку."}
:::

## Переход в подкаталог

Если нужный каталог находится внутри текущего, используйте относительный путь. Например, из `/home/pete/Pictures` в находящийся там каталог `Hawaii` можно перейти так:

```bash
$ cd Hawaii
```

Указано только имя папки, поскольку текущий каталог уже является её родителем `/home/pete/Pictures`.

## Основные сокращения навигации

Полные пути бывают громоздкими, поэтому оболочка предоставляет сокращения:

- `.` — текущий каталог;
- `..` — родительский каталог на один уровень выше;
- `~` — ваш домашний каталог, например `/home/pete`;
- `-` — предыдущий рабочий каталог.

Их можно передавать `cd`:

```bash
$ cd .
$ cd ..
$ cd ~
$ cd -
```

:::single-choice{#move-to-parent-directory} Какая команда из `/home/pete/Pictures` перемещает в `/home/pete`?

::option[`cd .`]{#cd-current explanation="`.` обозначает текущий каталог, поэтому оболочка останется в `/home/pete/Pictures`."}
::option[`cd -`]{#cd-previous explanation="`-` возвращает в предыдущий рабочий каталог, который не обязательно является родительским; для уровня вверх нужен `..`."}
::option[`cd ..`]{#cd-parent .correct explanation="`..` обозначает родителя текущего каталога; родитель `Pictures` — `/home/pete`."}
:::

:::single-choice{#return-to-previous-directory} Какая команда возвращает в каталог, использованный непосредственно перед текущим?

::option[`cd -`]{#previous-directory .correct explanation="`cd -` переключает в предыдущий рабочий каталог, который может находиться где угодно в файловой системе."}
::option[`cd ..`]{#parent-directory explanation="`cd ..` переходит к родителю; предыдущий и родительский каталоги не всегда совпадают."}
::option[`cd ~`]{#home-directory explanation="`cd ~` ведёт в домашний каталог и не отслеживает непосредственно предыдущее место."}
:::

Попрактикуйтесь с сокращениями, чтобы быстрее работать в командной строке.

## Практические примеры cd

Перейти в домашний каталог:

```bash
$ cd
```

`cd` без аргумента тоже переносит в домашний каталог.

Подняться на два уровня:

```bash
$ cd ../..
```

Перейти в каталог с пробелом в имени, заключив путь в кавычки:

```bash
$ cd "Vacation Photos"
```

:::single-choice{#enter-directory-with-spaces} Какая команда воспринимает `Vacation Photos` как одно имя каталога?

::option[`cd Vacation Photos`]{#unquoted-directory-name explanation="Без кавычек оболочка передаст `Vacation` и `Photos` как отдельные аргументы, а не одно имя."}
::option[`"cd Vacation Photos"`]{#quote-entire-command explanation="Кавычки вокруг всей строки заставят оболочку искать команду с таким полным именем; сама команда должна быть вне кавычек пути."}
::option[`cd "Vacation Photos"`]{#quote-directory-name .correct explanation="Кавычки объединяют два слова в один аргумент пути для `cd`."}
:::

Вернуться в предыдущий каталог:

```bash
$ cd -
/home/pete/Documents
```

Закрепить навигацию можно в лабораториях:

1. **[Команда Linux cd: смена каталога](https://labex.io/ru/labs/linux-linux-cd-command-directory-changing-209733)** — изучите разные способы перехода и пути.
2. **[Навигация по каталогам Linux](https://labex.io/ru/labs/linux-directory-navigation-387844)** — перемещайтесь по каталогам основными командами.
3. **[Создание структуры нового проекта](https://labex.io/ru/labs/linux-setting-up-a-new-project-structure-387859)** — создайте структуру через `mkdir` и переходите по ней через `cd`.

## Итоги

Теперь вы умеете перемещаться между каталогами с помощью `cd`, полных путей и сокращений оболочки.

1. Отличать абсолютные пути от относительных.
2. Менять каталог и проверять результат через `pwd`.
3. Переходить в родительский, домашний и предыдущий каталоги.
4. Открывать каталоги с пробелами в имени.
5. Распознавать распространённые ошибки пути и разрешений.
