---
lesson_id: "cat-command"
course_id: "command-line"
lang: "ru"
order_index: 7
title: "cat"
description: "Научитесь безопасно выводить, объединять и перенаправлять содержимое файлов командой cat."
meta_title: "cat - Командная строка"
meta_description: "Изучите команду Linux cat с примерами просмотра файлов, объединения файлов, нумерации строк, создания файлов и безопасного использования перенаправления."
meta_keywords: "linux cat команда, cat команда, просмотр файла linux, объединение файлов, cat -n, cat -b, cat перенаправление, linux cat"
---

После определения типа файла следующий шаг — прочитать его содержимое. Команда `cat` выводит и соединяет содержимое файлов; её имя сокращено от concatenate.

## Просмотр содержимого

Простейший вариант выводит файл прямо в терминал:

```bash
$ cat myfile.txt
```

Команда записывает весь файл в стандартный вывод. Это удобно для короткого текста, но длинный файл может слишком быстро прокрутиться.

:::single-choice{#display-short-file}
Какая команда выводит весь `myfile.txt` в терминал?

::option[`file myfile.txt`]{#classify-myfile explanation="`file` сообщает вероятный тип, но не печатает полный текст файла."}
::option[`touch myfile.txt`]{#update-myfile explanation="`touch` обновляет метки или создаёт отсутствующий файл и не показывает содержимое."}
::option[`cat myfile.txt`]{#display-myfile .correct explanation="`cat` читает `myfile.txt` и записывает его содержимое в стандартный вывод, здесь — терминал."}
:::

## Объединение файлов

Получив несколько файлов, `cat` читает их в порядке аргументов и выводит один за другим:

```bash
$ cat dogfile birdfile
```

Сначала появится `dogfile`, затем `birdfile`. Чтобы сохранить объединённый вывод в новый файл, перенаправьте стандартный вывод через `>`:

```bash
$ cat dogfile birdfile > animals
```

До запуска `cat` оболочка создаёт `animals` или обнуляет существующий файл, а затем направляет туда вывод. Не используйте входной файл как это назначение: он может быть очищен до чтения.

:::single-choice{#combine-files-in-order}
Какая команда записывает `part1`, затем `part2` в новый или заменяемый файл `whole`?

::option[`cat whole > part1 part2`]{#reverse-redirection explanation="У перенаправления одно назначение, а остальные слова становятся аргументами `cat`; нужный порядок входа и выхода здесь не задан."}
::option[`cat part1 part2 > whole`]{#ordered-inputs .correct explanation="`cat` выводит файлы в указанном порядке, а `>` направляет объединённый результат в `whole`."}
::option[`cat part2 part1 > whole`]{#reverse-inputs explanation="Файлы те же, но `part2` будет прочитан раньше `part1`; порядок аргументов определяет порядок вывода."}
:::

## Запись терминального ввода в файл

Без входного файла `cat` читает стандартный ввод. Вместе с `>` это позволяет набрать текст в терминале и записать его:

```bash
$ cat > newfile.txt
```

Введите текст, затем нажмите `Ctrl+D`, чтобы передать конец файла и вернуться в оболочку. Если `newfile.txt` уже существует, `>` удалит прежнее содержимое.

Для добавления используйте `>>`:

```bash
$ cat >> notes.txt
```

:::single-choice{#append-terminal-input}
Вы хотите дописать текст в конец существующего `notes.txt`. Какая команда начинает операцию без обнуления файла?

::option[`cat > notes.txt`]{#overwrite-notes explanation="Один `>` открывает назначение после обнуления, поэтому существующий текст будет потерян."}
::option[`cat >> notes.txt`]{#append-notes .correct explanation="Оператор `>>` открывает файл для добавления, и прочитанный `cat` текст помещается после существующего."}
::option[`cat notes.txt > notes.txt`]{#same-input-output explanation="Одинаковый вход и назначение `>` могут привести к обнулению до чтения; это не безопасное добавление."}
:::

## Форматирование вывода

Полезные параметры:

- `-n`: нумеровать все строки с 1.
- `-b`: нумеровать только непустые строки.
- `-s`: сжимать несколько пустых строк в одну.
- `-A`: показывать непечатаемые символы, табуляции и окончания строк.

Примеры:

```bash
$ cat -n script.sh
$ cat -b notes.txt
$ cat -s messy.txt
```

:::single-choice{#number-nonempty-lines}
Какая команда нумерует только непустые строки вывода `notes.txt`?

::option[`cat -b notes.txt`]{#number-nonblank .correct explanation="Параметр `-b` нумерует непустые строки и оставляет пустые без номера."}
::option[`cat -n notes.txt`]{#number-all-lines explanation="Параметр `-n` нумерует все строки, включая пустые, поэтому условие не выполняется."}
::option[`cat -s notes.txt`]{#squeeze-blank-lines explanation="Параметр `-s` сокращает повторные пустые строки до одной и не добавляет номера."}
:::

## Выбор просмотрщика для длинных файлов

Используйте `cat`, когда нужен весь вывод сразу. Для длинного файла удобнее `less`: он позволяет прокручивать, искать и выходить, не заполняя терминал.

```bash
$ less /var/log/syslog
```

:::single-choice{#choose-viewer-for-long-file}
Какая команда лучше подходит для интерактивного чтения длинного журнала?

::option[`less /var/log/syslog`]{#page-through-log .correct explanation="`less` предоставляет прокрутку, поиск и управляемый выход, поэтому удобен для длинных файлов."}
::option[`cat /var/log/syslog`]{#print-entire-log explanation="`cat` сразу печатает весь журнал, и длинный текст может прокрутиться до просмотра."}
::option[`touch /var/log/syslog`]{#update-log-time explanation="`touch` меняет временные метки и не читает журнал."}
:::

Для практики:

1. **[Команда Linux cat: объединение файлов](https://labex.io/ru/labs/linux-linux-cat-command-file-concatenating-210986)** — просматривайте и объединяйте текстовые файлы.
2. **[Просмотр журналов и конфигурации Linux](https://labex.io/ru/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** — применяйте `cat` к системным журналам и конфигурации.

## Итоги

Теперь вы умеете выводить и объединять содержимое через `cat`, безопасно выбирая перенаправление.

1. Показывать полное содержимое короткого файла.
2. Объединять файлы в выбранном порядке.
3. Осознанно заменять или дописывать назначение.
4. Нумеровать или упрощать строки вывода.
5. Выбирать `less` для интерактивного чтения.
