---
lesson_id: "less-command"
course_id: "command-line"
lang: "ru"
order_index: 8
title: "less"
description: "Научитесь интерактивно перемещаться, искать и отслеживать изменения в длинных текстовых файлах через less."
meta_title: "less - Командная строка"
meta_description: "Изучите команду Linux less с примерами просмотра больших файлов, прокрутки, поиска, перехода к строкам, слежения за журналами и выхода из less."
meta_keywords: "команда less, linux less, просмотр больших файлов linux, поиск в less, выход из less, less -N, less +F, просмотрщик текста linux"
---

Если текст не помещается на одном экране, `less` позволяет читать его без прокрутки всего файла через терминал. Название породило старую шутку Unix «less is more», поскольку `more` — другой постраничный просмотрщик.

## Открытие файла

Передайте просмотрщику имя файла:

```bash
$ less /home/pete/Documents/text1
```

Пока `less` активен, клавиши управляют просмотрщиком, а не запускают обычные команды оболочки. После выхода вы возвращаетесь в оболочку.

:::single-choice{#open-long-file} Какая команда открывает `/var/log/syslog` в интерактивном просмотрщике?

::option[`less /var/log/syslog`]{#page-log .correct explanation="`less` открывает файл для перемещения, поиска и последующего возврата в оболочку."}
::option[`cat /var/log/syslog`]{#print-log explanation="`cat` сразу отправляет весь файл в стандартный вывод и не предоставляет интерактивную навигацию."}
::option[`file /var/log/syslog`]{#classify-log explanation="`file` сообщает вероятный тип, но не открывает журнал для чтения."}
:::

## Навигация в less

Во время просмотра используйте:

- `Up`, `Down`, `Page Up` и `Page Down` для движения по строкам или экранам.
- `g` для перехода к началу.
- `G` для перехода к концу.
- `u` для движения на пол-экрана вверх и `d` — вниз.
- `h` для встроенной справки.

:::single-choice{#jump-to-file-end} Какая клавиша сразу переходит к концу файла в `less`?

::option[`g`]{#lowercase-g explanation="Строчная `g` ведёт к началу; прописная форма движется в противоположную сторону."}
::option[`G`]{#uppercase-g .correct explanation="Прописная `G` переходит в конец ввода; команда чувствительна к регистру."}
::option[`h`]{#help-key explanation="`h` открывает справку и не переходит к концу."}
:::

## Поиск в less

Введите `/`, шаблон и нажмите Enter для поиска вперёд. Символ `?` начинает поиск назад.

- `/search_term`: искать `search_term` вперёд.
- `?search_term`: искать назад.
- `n`: повторить в том же направлении.
- `N`: повторить в противоположном направлении.

:::single-choice{#repeat-search-direction} Какая клавиша после прямого поиска `error` повторяет его в том же направлении?

::option[`n`]{#same-search .correct explanation="Строчная `n` повторяет последний поиск в его исходном, здесь прямом, направлении."}
::option[`N`]{#opposite-search explanation="Прописная `N` повторяет поиск в обратном направлении, поэтому после прямого поиска движется назад."}
::option[`g`]{#search-to-start explanation="`g` переходит к началу ввода и не повторяет поиск."}
:::

## Выход из less

Нажмите `q`, чтобы закрыть `less` и вернуться к приглашению оболочки.

:::single-choice{#quit-less} Какая клавиша выходит из `less` и возвращает в оболочку?

::option[`q`]{#less-quit .correct explanation="Команда `q` закрывает просмотрщик и восстанавливает приглашение оболочки."}
::option[`h`]{#less-help explanation="`h` открывает справку внутри `less` и не возвращает прямо в оболочку."}
::option[`G`]{#less-end explanation="Прописная `G` движется в конец ввода, но просмотрщик остаётся открытым."}
:::

## Запуск less с параметрами

Параметры и начальные команды меняют способ запуска:

```bash
$ less -N file.txt
$ less +G file.txt
$ less +F /var/log/syslog
```

- `-N`: показывать номера строк.
- `+G`: открыть с конца.
- `+F`: следить за новым содержимым, подобно `tail -f`.

В режиме `+F` нажмите `Ctrl+C`, чтобы прекратить слежение и вернуться к навигации, затем `q` для выхода. Параметр `-i` игнорирует регистр, если в шаблоне нет прописных букв, а `-I` игнорирует его всегда.

В `less` можно передавать вывод команды через конвейер:

```bash
$ dmesg | less
```

:::single-choice{#follow-growing-log} Какая команда открывает `/var/log/syslog` и следит за поступающим содержимым?

::option[`less +F /var/log/syslog`]{#follow-log .correct explanation="Начальная команда `+F` включает режим слежения и показывает дописываемое содержимое."}
::option[`less +G /var/log/syslog`]{#open-at-log-end explanation="`+G` открывает конец, но не продолжает следить за будущими строками."}
::option[`less -N /var/log/syslog`]{#number-log-lines explanation="`-N` показывает номера строк и не включает непрерывное слежение."}
:::

Для практики:

1. **[Команда Linux less: постраничный просмотр](https://labex.io/ru/labs/linux-linux-less-command-file-paging-214301)** — изучите навигацию, поиск и номера строк.
2. **[Просмотр журналов и конфигурации Linux](https://labex.io/ru/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** — работайте с `cat`, `more` и `less`.

## Итоги

Теперь вы умеете исследовать длинные файлы через `less`, не переполняя терминал.

1. Открывать файл или вывод конвейера в просмотрщике.
2. Переходить к нужным частям ввода.
3. Искать вперёд и назад и повторять поиск.
4. Показывать номера строк или следить за ростом файла.
5. Безопасно выходить в оболочку.
