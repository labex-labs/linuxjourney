---
lesson_id: "pipe-tee-redirect"
course_id: "text-fu"
lang: "ru"
order_index: 4
title: "Конвейер и tee"
description: "Узнайте, как конвейеры соединяют команды, а tee сохраняет поток, одновременно передавая его дальше."
meta_title: "Конвейер и tee — обработка текста"
meta_description: "Изучите конвейеры и команду tee в Linux: объединяйте команды и направляйте вывод одновременно на экран и в файл."
meta_keywords: "конвейер и tee в Linux, Linux pipe tee, pipe to tee, конвейер Linux, команда tee, stdout, stdin, перенаправление командной строки"
---

Конвейеры соединяют небольшие команды, позволяя данным проходить между ними без промежуточного файла. Команда `tee` может сохранить копию части такого потока в файл и одновременно передать данные дальше.

## Соединение команд с помощью |

Предположим, список каталога слишком длинный, чтобы прочитать его целиком за один раз:

```bash
$ ls -la /etc
```

Поместите оператор конвейера `|` между командами, чтобы подключить stdout левой команды к stdin правой:

```bash
$ ls -la /etc | less
```

Оболочка запускает команды конвейера и настраивает соединение потоков. Команды могут работать одновременно: `less` начнёт чтение ещё до того, как `ls` сформирует весь список.

:::single-choice{#pipe-stream-connection} Какие потоки по умолчанию соединяет `|` в `ls -la /etc | less`?

::option[stdin команды `ls` со stdout команды `less`.]{#pipe-reversed-streams explanation="Здесь перепутаны и производитель, и потребитель. Данные идут из вывода левой команды во ввод правой."}
::option[stderr команды `ls` с обоими потоками `less`.]{#pipe-stderr-both explanation="Обычный конвейер не подключает stderr левой команды и не направляет данные в оба потока правой."}
::option[stdout команды `ls` со stdin команды `less`.]{#pipe-stdout-stdin .correct explanation="Стандартный конвейер соединяет файловый дескриптор 1 левой команды с файловым дескриптором 0 правой."}
:::

## Отдельный stderr

Обычный `|` передаёт только stdout. Stderr левой команды сохраняет прежнее назначение, которым часто является терминал:

```bash
$ find /etc -name "*.conf" | less
```

Найденные пути проходят через конвейер, а диагностические сообщения о правах доступа могут по-прежнему появляться прямо в терминале. Если нужно другое поведение, перенаправьте stderr отдельно:

```bash
$ find /etc -name "*.conf" 2> find-errors.log | less
```

:::single-choice{#pipe-left-stderr} Куда обычно направляется stderr команды `find` в `find /etc -name "*.conf" | less`, если других перенаправлений нет?

::option[В `less` через тот же конвейер, что и stdout.]{#pipe-errors-to-less explanation="Обычный конвейер подключает только stdout. Stderr не объединяется с ним автоматически."}
::option[В файл `stderr` в текущем каталоге.]{#pipe-errors-to-file explanation="Перенаправление в файл ошибок не указано, поэтому оболочка не создаёт такой файл."}
::option[В прежнее назначение, обычно в терминал.]{#pipe-errors-terminal .correct explanation="Поскольку дескриптор 2 не изменён, диагностические сообщения обычно остаются подключёнными к терминалу."}
:::

## Копирование потока с помощью tee

`tee` читает stdin, записывает копию в каждый указанный файл и одновременно передаёт те же данные в stdout:

```bash
$ ls | tee listing.txt
```

Здесь `listing.txt` получает список, а stdout команды `tee` остаётся подключённым к терминалу. По умолчанию `tee` создаёт или усекает указанный файл, как и `>`.

:::single-choice{#tee-display-and-save} Какая команда отображает вывод `generate-report` и одновременно заменяет им содержимое `report.txt`?

::option[`generate-report > report.txt`]{#redirect-report-only explanation="Обычное перенаправление вывода записывает файл, но не оставляет копию потока для терминала."}
::option[`generate-report | tee report.txt`]{#tee-report .correct explanation="`tee` копирует stdin в `report.txt` и в свой stdout, который в этом конвейере остаётся терминалом."}
::option[`tee generate-report | report.txt`]{#tee-operands-reversed explanation="Здесь `generate-report` считается именем файла назначения, а `report.txt` пытаются выполнить как команду. Производитель должен находиться слева."}
:::

Используйте `-a`, если данные нужно добавить в конец файла, а не заменить его:

```bash
$ date | tee -a activity.log
```

:::single-choice{#tee-append-log} Какая команда отображает текущую дату и добавляет её в `activity.log`?

::option[`date | tee -a activity.log`]{#tee-append-activity .correct explanation="Параметр `-a` заставляет `tee` добавлять данные в файл, продолжая копировать ввод в stdout."}
::option[`date | tee activity.log`]{#tee-replace-activity explanation="Без `-a` команда `tee` заменяет существующий файл, а не сохраняет предыдущие записи."}
::option[`date > activity.log`]{#redirect-replace-activity explanation="Эта команда заменяет файл и не отправляет копию в терминал, поэтому не выполняет ни требование добавления, ни требование отображения."}
:::

## Сохранение промежуточного результата

Поместите `tee` в середину конвейера, чтобы сохранить промежуточный поток и продолжить его обработку:

```bash
$ ls -la /etc | tee etc-listing.txt | grep "conf"
```

Этот конвейер:

1. Формирует полный подробный список.
2. Сохраняет весь поток в `etc-listing.txt`.
3. Передаёт тот же поток в `grep`, который выводит только строки с `conf`.

Файл содержит данные до фильтрации командой `grep`. Если в файл нужны только отфильтрованные строки, поместите `tee` после `grep`.

:::single-choice{#tee-before-filter-result} Что будет содержать `all.txt` после успешного выполнения `produce | tee all.txt | grep error`?

::option[Только строки, совпавшие с условием `grep`.]{#tee-filtered-only explanation="`tee` выполняется до `grep`, поэтому записывает неотфильтрованный ввод, а не набор последующих совпадений."}
::option[Только stderr команды `produce`.]{#tee-producer-stderr explanation="Обычный конвейер передаёт stdout команды `produce`. Её stderr не является вводом `tee`."}
::option[Весь stdout, сформированный до фильтрации.]{#tee-complete-intermediate .correct explanation="`tee` сохраняет каждый полученный байт, а затем передаёт тот же поток в `grep` для фильтрации."}
:::

Чтобы попрактиковаться с конвейерами и копированием потоков, выполните лабораторные работы:

1. **[Перенаправление ввода и вывода в Linux](https://labex.io/ru/labs/comptia-redirecting-input-and-output-in-linux-590840)** — потренируйтесь управлять stdout, stderr и stdin с помощью операторов `>`, `>>`, `2>` и команды `tee`.
2. **[Управление последовательностью и конвейеры](https://labex.io/ru/labs/linux-sequence-control-and-pipeline-17994)** — научитесь управлять последовательностями команд, использовать конвейеры и инструменты обработки текста `cut`, `grep`, `wc`, `sort` и `uniq`.
3. **[Перенаправление потоков данных](https://labex.io/ru/labs/linux-data-stream-redirection-17995)** — освойте управление стандартными потоками ввода, вывода и ошибок, объединение вывода и применение `/dev/null`.

## Итоги

Теперь вы можете соединять команды и сохранять выбранные точки потока данных.

1. Передавайте stdout одной команды в stdin другой через конвейер.
2. При необходимости перенаправляйте stderr отдельно.
3. Копируйте ввод одновременно в файл и stdout с помощью `tee`.
4. Добавляйте данные командой `tee -a`, не заменяя файл.
5. Осознанно размещайте `tee` до или после фильтра.
