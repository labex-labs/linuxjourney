---
lesson_id: "stderr-standard-error-redirect"
course_id: "text-fu"
lang: "ru"
order_index: 3
title: "stderr (стандартный поток ошибок)"
description: "Научитесь перенаправлять стандартный поток ошибок отдельно или объединять его со стандартным выводом в Bash."
meta_title: "stderr (стандартный поток ошибок) — обработка текста"
meta_description: "Научитесь управлять stderr в Linux: перенаправлять файловый дескриптор 2 в файл или /dev/null с помощью 2>, 2>&1 и &>."
meta_keywords: "stderr, стандартный поток ошибок Linux, файловый дескриптор stderr, файл stderr, перенаправление stderr, 2>, 2>&1, &>, /dev/null, обработка ошибок Bash"
---

Обычные результаты программы, как правило, записывают в стандартный вывод, а диагностические сообщения — в отдельный стандартный поток ошибок, или **stderr**. Разделение потоков позволяет сохранять полезные данные, не смешивая их с сообщениями об ошибках.

## Отделение обычного вывода от ошибок

Рассмотрим команду с несуществующим путём:

```bash
$ ls /fake/directory > peanuts.txt
ls: cannot access '/fake/directory': No such file or directory
```

Оператор `>` перенаправляет только stdout. Диагностическое сообщение записывается в stderr, который всё ещё подключён к терминалу. Тем временем оболочка создаёт или усекает `peanuts.txt` для stdout, хотя `ls` не формирует обычного результата.

Стандартные потоки по соглашению используют следующие файловые дескрипторы:

- `0`: stdin (стандартный ввод)
- `1`: stdout (стандартный вывод)
- `2`: stderr (стандартный поток ошибок)

:::single-choice{#stderr-not-in-stdout-file} Почему ошибка от `ls /missing > results.txt` обычно остаётся в терминале?

::option[`>` перенаправляет stdout, а диагностическое сообщение записывается в stderr.]{#stderr-separate-stream .correct explanation="Обычный `>` изменяет только файловый дескриптор 1. Дескриптор 2 сохраняет прежнее назначение — терминал."}
::option[`ls` ждёт закрытия файла, прежде чем вывести ошибку.]{#stderr-waits-for-close explanation="Дело не во времени: обычные и диагностические сообщения используют разные выходные потоки."}
::option[`results.txt` может хранить обычный текст, но не диагностические сообщения.]{#stderr-file-capability explanation="Обычный файл может хранить любой из потоков. В командной строке просто не было перенаправления stderr в него."}
:::

## Перенаправление stderr с помощью 2>

Чтобы перенаправить stderr, укажите файловый дескриптор `2` перед `>`:

```bash
$ ls /fake/directory 2> errors.txt
```

Оболочка создаёт или усекает `errors.txt` и подключает его к дескриптору 2. Stdout сохраняет прежнее назначение. Если вывод ошибок нужно добавить в конец файла, используйте `2>> errors.txt`.

:::single-choice{#stderr-to-error-file} Какая команда заменяет `errors.log` диагностическими сообщениями от `find /restricted`, оставляя stdout в прежнем назначении?

::option[`find /restricted > errors.log`]{#stdout-errors-log explanation="Обычный `>` перенаправляет дескриптор 1, поэтому захватывает обычные результаты, а не специально диагностические сообщения."}
::option[`find /restricted < errors.log`]{#stdin-errors-log explanation="Знак меньше предоставляет файл через stdin и не захватывает ни один выходной поток."}
::option[`find /restricted 2> errors.log`]{#stderr-errors-log .correct explanation="Начальная `2` выбирает stderr, а `>` создаёт или усекает назначение для этого потока."}
:::

## Объединение stdout и stderr

Чтобы поместить оба выходных потока в один файл, сначала перенаправьте stdout, а затем продублируйте stderr в текущее назначение stdout:

```bash
$ ls /fake/directory /etc/passwd > combined.txt 2>&1
```

Перенаправления обрабатываются слева направо:

1. `> combined.txt` подключает stdout к файлу.
2. `2>&1` подключает stderr туда, куда в этот момент направлен stdout.

Обратный порядок изменяет результат:

```bash
$ ls /fake/directory /etc/passwd 2>&1 > regular.txt
```

Здесь stderr сначала дублирует исходное назначение stdout — терминал. Затем stdout перемещается в `regular.txt`, поэтому два потока оказываются в разных местах.

:::single-choice{#stderr-combine-order} Какое перенаправление Bash отправляет и stdout, и stderr команды `command` в `all.log`?

::option[`command 2>&1 > all.log`]{#stderr-before-stdout explanation="Сначала stderr подключается к старому назначению stdout, затем только stdout перенаправляется в файл. Потоки оказываются разделены."}
::option[`command 2> all.log > /dev/null`]{#stderr-file-stdout-null explanation="Здесь stderr направляется в `all.log`, а stdout отбрасывается. Оба потока в файле не объединяются."}
::option[`command > all.log 2>&1`]{#stdout-then-stderr .correct explanation="Сначала stdout направляется в файл, затем stderr дублирует это текущее назначение stdout."}
:::

Bash также предоставляет `&>` как сокращённую запись для замены файла обоими потоками:

```bash
$ ls /fake/directory /etc/passwd &> combined.txt
```

Для добавления обоих потоков в Bash используйте `&>>`. Явную форму `> file 2>&1` полезно узнавать, поскольку она также встречается в сценариях оболочки и документации.

:::single-choice{#stderr-bash-short-form} Какая команда Bash добавляет и stdout, и stderr команды `build` в `build.log`?

::option[`build &> build.log`]{#replace-both-build explanation="В Bash `&>` перенаправляет оба потока, но заменяет существующий файл вместо добавления."}
::option[`build 2>> build.log`]{#append-errors-build explanation="Эта запись добавляет только stderr. Stdout сохраняет прежнее назначение."}
::option[`build &>> build.log`]{#append-both-build .correct explanation="В Bash `&>>` добавляет файловые дескрипторы 1 и 2 в одно назначение."}
:::

## Намеренное отбрасывание потока

`/dev/null` — специальное устройство, отбрасывающее записанные в него данные. Перенаправляйте туда stderr, только если установили, что диагностические сообщения ожидаемы и не нужны:

```bash
$ ls /fake/directory 2> /dev/null
```

Это не заставляет команду завершиться успешно и не изменяет её код выхода, а лишь скрывает диагностический поток. При поиске неисправностей сохраняйте или отображайте stderr, а не отбрасывайте нужную информацию.

:::single-choice{#stderr-dev-null-effect} Что изменяет `check-data 2> /dev/null`?

::option[Отбрасывает stdout и превращает каждую ошибку в успешное завершение.]{#discard-stdout-success explanation="Дескриптор 2 — это stderr, а не stdout; перенаправление не переписывает код выхода программы."}
::option[Отбрасывает stderr, но не заставляет команду завершиться успешно.]{#discard-stderr-only .correct explanation="Перенаправление меняет место назначения диагностических сообщений. Результат выполнения по-прежнему определяет программа."}
::option[Сохраняет stderr в скрытом файле `/dev/null`.]{#save-dev-null explanation="`/dev/null` отбрасывает записанные данные и не хранит их для последующего восстановления."}
:::

Чтобы попрактиковаться в управлении всеми тремя стандартными потоками, выполните лабораторную работу:

1. **[Перенаправление ввода и вывода в Linux](https://labex.io/ru/labs/comptia-redirecting-input-and-output-in-linux-590840)** — научитесь перенаправлять ввод и вывод в оболочке Linux, управляя stdout, stderr и stdin с помощью операторов `>`, `>>`, `2>` и команды `tee`.

## Итоги

Теперь вы можете отделять диагностические сообщения от обычного вывода команды или объединять эти потоки.

1. Распознавайте stderr как файловый дескриптор 2.
2. Заменяйте журнал ошибок или добавляйте в него с помощью `2>` и `2>>`.
3. Применяйте несколько перенаправлений слева направо.
4. Объединяйте оба выходных потока с помощью осознанно выбранного синтаксиса.
5. Отбрасывайте диагностические сообщения, только если их потеря допустима.
