---
lesson_id: "stdin-standard-in-redirect"
course_id: "text-fu"
lang: "ru"
order_index: 2
title: "stdin (стандартный ввод)"
description: "Узнайте, как программы читают стандартный ввод и как Bash подключает этот поток к файлу."
meta_title: "stdin (стандартный ввод) — обработка текста"
meta_description: "Освойте перенаправление stdin в командной строке Linux: связь stdin и stdout, оператор < и практические примеры управления потоками данных."
meta_keywords: "stdin, стандартный ввод, перенаправление stdin, cat stdin, stdin и stdout, перенаправление Linux, командная строка, поток ввода"
---

Стандартный ввод, сокращённо **stdin**, — это поток, из которого программа обычно читает входящие данные. В интерактивном терминале оболочка, как правило, подключает stdin к вводу терминала, поэтому программа может читать набираемый вами текст.

## Стандартный ввод и файловый дескриптор 0

По соглашению три стандартных потока используют следующие номера файловых дескрипторов:

- `0`: стандартный ввод (`stdin`)
- `1`: стандартный вывод (`stdout`)
- `2`: стандартный поток ошибок (`stderr`)

Программа сама выбирает, использовать ли эти потоки и каким образом. Команда, предназначенная для чтения stdin, часто ожидает ввод из терминала, если ей не передан файл или другой источник данных.

:::single-choice{#stdin-descriptor-number}
Какой файловый дескриптор по соглашению обозначает стандартный ввод?

::option[`0`]{#stdin-fd-zero .correct explanation="Стандартный ввод по соглашению соответствует файловому дескриптору 0."}
::option[`1`]{#stdin-fd-one explanation="Файловый дескриптор 1 по соглашению обозначает стандартный вывод — поток обычных результатов."}
::option[`2`]{#stdin-fd-two explanation="Файловый дескриптор 2 по соглашению обозначает стандартный поток ошибок, а не стандартный ввод."}
:::

## Перенаправление файла в stdin

Оператор `<` указывает Bash открыть файл для чтения и подключить его к stdin команды:

```bash
$ cat < peanuts.txt
Hello World
```

Оболочка обрабатывает `< peanuts.txt`, а `cat` просто читает файловый дескриптор 0. Путь не передаётся `cat` как обычный файловый операнд.

Если входной файл не существует или его нельзя открыть, оболочка сообщает об ошибке перенаправления и не запускает команду с таким вводом.

:::single-choice{#stdin-from-file}
Какая команда заставляет `sort` читать стандартный ввод из `names.txt`?

::option[`sort < names.txt`]{#sort-stdin-file .correct explanation="Bash открывает `names.txt` для чтения и подключает его к `sort` через файловый дескриптор 0."}
::option[`sort > names.txt`]{#stdout-to-names explanation="Знак больше перенаправляет stdout в файл и может усечь его. Он не предоставляет файл как ввод."}
::option[`sort names.txt >`]{#incomplete-sort-output explanation="Здесь указано незавершённое перенаправление вывода, а не нужное подключение stdin."}
:::

## Файловый операнд и перенаправление ввода

Некоторые команды принимают как имя файла, так и stdin, но результаты могут немного различаться. Например:

```bash
$ wc -l peanuts.txt
1 peanuts.txt
$ wc -l < peanuts.txt
1
```

Обе формы подсчитывают строки в одних и тех же данных. В первой форме `wc` знает имя файла, потому что получает его как аргумент. Во второй команда получает только поток через stdin, поэтому ей нечего выводить в качестве имени файла.

:::single-choice{#stdin-not-command-argument}
Почему `wc -l < peanuts.txt` обычно не выводит `peanuts.txt`?

::option[`wc` удаляет имя файла после подсчёта строк.]{#stdin-delete-name explanation="Команда не переименовывает и не удаляет исходный файл — изменяется только подключение ввода."}
::option[Оператор `<` скрывает каждое слово, выводимое командой.]{#stdin-hide-words explanation="Перенаправление ввода не фильтрует stdout. Имя отсутствует, потому что `wc` не получила его как аргумент."}
::option[Bash предоставляет файл через stdin, а не как аргумент с именем файла.]{#stdin-no-filename .correct explanation="Оболочка обрабатывает перенаправление и подключает файл к дескриптору 0, поэтому путь не передаётся `wc` как операнд."}
:::

## Сочетание перенаправлений ввода и вывода

В одной командной строке можно перенаправить несколько потоков:

```bash
$ cat < peanuts.txt > banana.txt
```

Оболочка создаёт два независимых соединения:

1. `< peanuts.txt` открывает `peanuts.txt` как stdin команды `cat`.
2. `> banana.txt` создаёт или усекает `banana.txt` и подключает его к stdout команды `cat`.

`cat` читает байты из stdin и записывает их в stdout, поэтому `banana.txt` получает содержимое исходного файла. Для обычного копирования файлов команда `cp peanuts.txt banana.txt` яснее выражает намерение; данный пример иллюстрирует соединения потоков.

:::single-choice{#stdin-and-stdout-files}
Какой файл в `cat < input.txt > output.txt` предоставляет stdin, а какой получает stdout?

::option[`output.txt` предоставляет stdin, а `input.txt` получает stdout.]{#stdin-output-stdout-input explanation="Здесь значения операторов перенаправления перепутаны. Для ввода стрелка направлена к команде, а для вывода — к файлу."}
::option[`input.txt` предоставляет stdin, а `output.txt` получает stdout.]{#stdin-input-stdout-output .correct explanation="Перенаправление `<` открывает `input.txt` для дескриптора 0, а `>` открывает `output.txt` для дескриптора 1."}
::option[Оба файла предоставляют stdin, а stdout остаётся в терминале.]{#both-stdin explanation="Два оператора воздействуют на разные стандартные потоки. `>` перенаправляет stdout из терминала."}
:::

Чтобы попрактиковаться в перенаправлении ввода и вывода, выполните лабораторные работы:

1. **[Перенаправление ввода и вывода в Linux](https://labex.io/ru/labs/comptia-redirecting-input-and-output-in-linux-590840)** — потренируйтесь управлять потоками данных команд, перенаправляя стандартный вывод (stdout), стандартный поток ошибок (stderr) и стандартный ввод (stdin) с помощью операторов `>`, `>>`, `2>` и команды `tee`.
2. **[Перенаправление потоков данных](https://labex.io/ru/labs/linux-data-stream-redirection-17995)** — научитесь управлять стандартными потоками ввода, вывода и ошибок, объединять вывод и использовать `/dev/null` для более сложных операций с файлами.

## Итоги

Теперь вы можете подключать стандартный ввод команды к файлу средствами оболочки.

1. Распознавайте stdin как файловый дескриптор 0.
2. Перенаправляйте доступный для чтения файл с помощью `<`.
3. Отличайте файловый операнд от перенаправленного ввода.
4. Осознанно сочетайте перенаправления stdin и stdout.
