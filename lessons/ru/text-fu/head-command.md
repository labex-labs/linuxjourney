---
lesson_id: "head-command"
course_id: "text-fu"
lang: "ru"
order_index: 8
title: "head"
description: "Научитесь выводить заданное количество строк или байтов с начала входных данных."
meta_title: "head — обработка текста"
meta_description: "Руководство для начинающих по команде head в Linux: просматривайте начало файла и задавайте число строк параметром head -n."
meta_keywords: "команда head, Linux head, просмотр начала файла, команды Linux, Linux для начинающих, head -n, текстовые файлы, командная строка"
---

Команда `head` выводит начало файла или входного потока. Она удобна для проверки заголовков, предварительного просмотра структурированных данных и выборки части вывода без печати целиком.

## Вывод первых десяти строк

Без параметра количества `head` выводит первые 10 строк каждого указанного файла:

```bash
$ head events.log
```

Файл при этом не изменяется. Если в нём меньше 10 строк, выводятся все доступные строки.

:::single-choice{#head-default-lines} Что по умолчанию выводит `head events.log`?

::option[Последние 10 строк либо все строки, если файл короче.]{#head-last-ten explanation="Вывод конца входных данных — задача `tail`; `head` выбирает начало."}
::option[Первые 10 строк либо все строки, если файл короче.]{#head-first-ten .correct explanation="Без параметра количества `head` выбирает до десяти первых строк входных данных."}
::option[Только первую строку независимо от длины файла.]{#head-first-one explanation="Для одной строки нужно явно указать число, например `-n 1`; по умолчанию выбирается десять."}
:::

## Выбор количества строк

Используйте `-n NUMBER`, чтобы задать число выводимых строк:

```bash
$ head -n 15 events.log
```

GNU `head` также принимает компактную форму `-15`, однако `-n 15` яснее выражает назначение параметра.

:::single-choice{#head-five-lines} Какая команда выводит первые пять строк `report.txt`?

::option[`head -c 5 report.txt`]{#head-five-bytes explanation="Параметр `-c` считает байты, а не строки, поэтому вывод может закончиться внутри первой строки."}
::option[`head -n 5 report.txt`]{#head-report-five .correct explanation="Параметр `-n` задаёт количество строк, а `5` запрашивает первые пять."}
::option[`tail -n 5 report.txt`]{#tail-five-lines explanation="Эта команда выводит последние пять строк файла, а не начало."}
:::

## Выбор количества байтов

Используйте `-c NUMBER`, когда нужны байты, а не полные строки:

```bash
$ head -c 20 archive.bin
```

Команда выводит первые 20 байтов. Вывод может завершиться посередине текстовой строки или, для многобайтового текста, внутри закодированного символа. Для обычного предварительного просмотра текста используйте режим строк.

:::single-choice{#head-first-bytes} Какая команда записывает первые 100 байтов `payload.bin` в stdout?

::option[`head -c 100 payload.bin`]{#head-hundred-bytes .correct explanation="Параметр `-c` выбирает количество байтов, поэтому запрашиваются первые 100 доступных байтов."}
::option[`head -n 100 payload.bin`]{#head-hundred-lines explanation="Параметр `-n` считает строки, а не байты, и может вывести намного больше или меньше 100 байтов."}
::option[`cut -c 100 payload.bin`]{#cut-hundredth-character explanation="Эта команда выбирает позицию 100 в каждой строке, а не первые 100 байтов всего ввода."}
:::

## Чтение stdin и нескольких файлов

Если файловый операнд не указан, `head` читает stdin:

```bash
$ generate-report | head -n 5
```

Когда указано несколько файлов, `head` обычно добавляет заголовок, обозначающий вывод каждого из них:

```bash
$ head -n 2 january.txt february.txt
==> january.txt <==
...

==> february.txt <==
...
```

Используйте `-q`, чтобы подавить заголовки, или `-v`, чтобы показать заголовок даже для одного файла.

:::single-choice{#head-pipeline-preview} Что читает `head` в `generate-report | head -n 5`?

::option[Stdout команды `generate-report` через stdin.]{#head-pipe-input .correct explanation="Конвейер подключает stdout производителя к stdin команды `head`, которая выбирает первые пять строк."}
::option[Первые пять имён файлов в текущем каталоге.]{#head-directory-names explanation="Команда просмотра каталога отсутствует; `head` получает поток через конвейер."}
::option[Пять байтов из файла `generate-report`.]{#head-producer-file explanation="Левая часть выполняется как команда, а `-n` считает строки, а не байты."}
:::

:::single-choice{#head-suppress-filename-headers} Какой параметр подавляет заголовки с именами файлов, когда `head` читает несколько файлов?

::option[`-v`]{#head-verbose explanation="Параметр `-v` требует заголовки даже для одного файла — это противоположное действие."}
::option[`-c`]{#head-byte-option explanation="Параметр `-c` переключает единицу выбора на байты и не управляет заголовками файлов."}
::option[`-q`]{#head-quiet .correct explanation="Параметр `-q`, или quiet, запрещает `head` печатать метки-заголовки отдельных файлов."}
:::

Чтобы попрактиковаться в просмотре начала файлов, выполните лабораторные работы:

1. **[Команда head в Linux: вывод начала файла](https://labex.io/ru/labs/linux-linux-head-command-file-beginning-display-214302)** — научитесь выводить начальные строки текстовых файлов и изменять их количество.
2. **[Просмотр журналов и конфигурационных файлов в Linux](https://labex.io/ru/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** — потренируйтесь эффективно просматривать текстовые файлы, включая системные журналы и конфигурацию.
3. **[Быстрое обнаружение угроз](https://labex.io/ru/labs/linux-rapid-threat-detection-387930)** — примените `head` и `tail` для быстрого извлечения и анализа записей журналов в сценарии кибербезопасности.

## Итоги

Теперь вы можете просматривать начало файлов и вывода команд с помощью `head`.

1. Используйте стандартный вывод первых десяти строк.
2. Задавайте число строк параметром `-n`.
3. При необходимости задавайте число байтов параметром `-c`.
4. Читайте stdin в конвейере.
5. Управляйте заголовками при выводе нескольких файлов.
